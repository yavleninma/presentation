{
  "name": "slideforge-presentations",
  "private": true,
  "description": "SlideForge monorepo — presentations web app (Next.js); run npm install here once for Git hooks",
  "scripts": {
    "prepare": "husky",
    "lint": "npm run lint --prefix presentations-frontend",
    "typecheck": "npm run typecheck --prefix presentations-frontend",
    "build": "npm run build --prefix presentations-frontend",
    "verify": "npm run lint --prefix presentations-frontend && npm run typecheck --prefix presentations-frontend && npm run build --prefix presentations-frontend",
    "verify:quick": "npm run lint --prefix presentations-frontend && npm run typecheck --prefix presentations-frontend"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
