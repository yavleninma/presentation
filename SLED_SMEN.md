# SLED_SMEN.md

Единый журнал следов рабочих чатов.

Один чат = одна запись.
Одна запись = один номер.
Заголовок записи = короткое имя чата, которое можно вручную перенести в название треда.

## Как вести журнал

1. Первое рабочее действие нового чата: открыть этот файл, взять следующий свободный номер, придумать короткое имя из первого сообщения пользователя и сразу создать запись.
2. Короткое имя делается максимально коротким. Обычно 2-4 слова. Формат заголовка: `### 001 Журнал следов`.
3. Сразу после заголовка агент заводит черновой след в той же записи, даже если основная работа ещё не началась.
4. Только после этого агент читает другие файлы, собирает контекст и делает основную задачу.
5. В том же чате нельзя заводить второй номер. Запись всегда переписывается как общий итог всего чата на текущий момент.
6. Чужие записи не редактировать. Закрытые чаты не трогать.
7. Если в чате всплыли несколько близких решений, держать одну основную суть, а остальные перечислять кратко в той же записи.
8. Если работа явно ушла в другую рабочую задачу, агент мягко подсказывает, что для новой дороги лучше открыть новый чат.
9. Перед долгим расследованием технической или продуктовой проблемы агент быстро просматривает блоки `Проблемы` ниже, чтобы не повторять уже пройденный путь.
10. Если долгой проблемы раньше не было, агент после решения кратко дописывает её в свой блок `Проблемы`: что произошло и как вышли.
11. Формулировки держать короткими и рабочими. Без витрины, без самоописания, без лишней истории.

## Формат записи

- `### 001 Журнал следов`
- `**Что уложено** ...`
- `**Статус слоя** Мутно / Идти можно / Внятно`
- `**Что стало внятнее** ...`
- `**Что ещё мутно** ...`
- `**Передача смены** ...`
- `**Проблемы**`
- `- Нет.` или короткие пункты вида `что было -> как решили`

Для самого первого чернового следа в новом чате достаточно коротких формулировок:

- `**Что уложено** След открыт. Идёт работа.`
- `**Статус слоя** Идти можно`
- `**Что стало внятнее** Зафиксированы номер и короткое имя чата.`
- `**Что ещё мутно** Основная работа ещё впереди.`
- `**Передача смены** После содержательной работы обновить эту же запись.`
- `**Проблемы**`
- `- Нет.`

## Записи

### 054 Коммит в main

**Что уложено**
- След открыт: пользователь попросил закоммитить все изменения и запушить в `main`.
- В корневой `.gitignore` добавлена папка `refs/` (локальные PPTX-референсы не в репозиторий).
- Коммит `67954c9` (`054.1`): прототип `presentations-frontend` (модульные стили, `/api/upload`, `/api/images`, PDF-экспорт, тесты), обновления журнала и корневого lockfile; удалён каталог `old/` и `ARCHIVE_MAP.md`. Пуш в `origin/main`.

**Статус слоя** Идти можно

**Что стало внятнее** Состояние репозитория на `main` синхронизировано с удалённым; `npm run verify` в `presentations-frontend` перед коммитом — ок.

**Что ещё мутно** —

**Передача смены** При необходимости восстановить архив SlideForge — только из истории git до `67954c9^`, не из рабочей копии.

**Проблемы**

- Нет.

---

### 053 Запуск dev

**Что уложено**
- Запущен `npm run dev` в `presentations-frontend` (Next.js 16.2.2, Turbopack).
- В `next.config.ts` добавлена загрузка env из корня репозитория (`loadEnvConfig(..)` + `loadEnvConfig(presentations-frontend)`), чтобы `OPENAI_API_KEY` из `../.env.local` попадал в `/api/draft` без дублирования файла.
- В `.env.example` указан `OPENAI_API_KEY` и пояснение про корень репо.

**Статус слоя** Идти можно

**Что стало внятнее** Локальный адрес: http://localhost:3000; черновик через API после перезапуска dev должен собираться при ключе только в корневом `.env.local`.

**Что ещё мутно** —

**Передача смены** Dev перезапущен после `.env.local` в `presentations-frontend/`; проверить сценарий «Показ продукта» → уточнение → сборка.

**Проблемы**

- Ручной прогон в браузере: без ключа в env `/api/draft` (generate) отдавал 500 и алерт «OPENAI_API_KEY не настроен» — причина: Next читал только `presentations-frontend/.env*`, ключ лежал в корне репо.

---

### 052 Неделя 3 — Вход + качество

**Что уложено**
- Расширена модель `DraftSession`: добавлены `uploadedContent`, `uploadedFileName`, `clarifyAnswers` (новый тип `ClarifyAnswers`).
- `normalizeDraftSession` расширен: валидирует `modern`/`corporate` templateId и `rose`/`emerald`/`violet`/`zinc` colorThemeId (побочный фикс Недели 2).
- `buildSessionContext` передаёт upload-контент (до 8000 символов), clarify-ответы и уточнённый стиль/аудиторию в промпт.
- Серверный API `/api/upload` (POST): mammoth → DOCX, pdf-parse → PDF, JSZip → PPTX, обрезка до 12000 символов.
- UI на start-screen: компонент `FileUploadZone` (drag-and-drop + input[type=file]), счётчик символов textarea, расширение `max-height` до 20rem.
- Clarify-экран полностью переписан с чат-интерфейса на 5-шаговую форму (audience, length, style, data, outcome), результат попадает в `session.clarifyAnswers`.
- `draft-adapter.ts` переписан: двухэтапная генерация (1. `PLAN_SYSTEM` → структура, 2. `GENERATE_SYSTEM` → наполнение), few-shot примеры, стилевые правила в системных промптах.
- `building-screen.tsx` обновлён: 4 реальных шага (Анализирую → Планирую → Наполняю → Готово) вместо таймерной имитации; прогресс передаётся через `buildingStep` пропс.
- `npm run verify` чист: lint ✓, typecheck ✓, 29/29 тестов ✓, build ✓.

**Статус слоя** Внятно

**Что стало внятнее**
- Пользователь может загрузить DOCX/PDF/PPTX — текст попадает в контекст AI.
- Clarify стал структурированной формой вместо непредсказуемого чата.
- AI теперь сначала планирует структуру, потом наполняет слайды — качество контента лучше.
- Промпты содержат стилевые правила и few-shot примеры.

**Что ещё мутно**
- Building-screen прогресс привязан к таймерам (не к реальным этапам сервера) — для MVP достаточно.
- pdf-parse может плохо парсить сканированные PDF — ограничение зафиксировано в плане.

**Передача смены** Следующий чат — Неделя 4: экспорт и полировка.

**Проблемы**
- pdf-parse v2 имеет другой export, чем v1 (нет `.default`). Решение: динамический импорт с runtime-проверкой типа (`typeof pdfModule === "function"`).
- pdf-parse@2.4.5 требует Node >=20.16.0, стоит 20.15.1 — предупреждение при npm install, в runtime не влияет.

---

### 051 Неделя 2 — Типы слайдов

**Что уложено**
- 10 новых layout-ов в SlideCanvas: section-divider, text-block, cards-row, list-slide, timeline, chart-bar, chart-progress, table-simple, image-text, closing.
- CSS для всех 10 layout-ов + template-specific overrides (strict, cards, briefing, modern, corporate).
- 4 новые цветовые темы: rose, emerald, violet, zinc — с полным набором: canvas, briefing dark, pill dots, segmented dots, rail thumbnails.
- 2 новых шаблона: modern (крупная типографика, без теней), corporate (строгие линии, чёткая сетка).
- Unsplash API: /api/images route, imageQuery в промпте генерации, backgroundImage/imageCredit в PresentationSlide, overlay CSS.
- next.config.ts с remotePatterns для images.unsplash.com.
- Генератор обновлён: slideFunctionToLayout (section→section-divider, closing→closing), chooseBestLayout (chart-bar, list-slide, cards-row, text-block, timeline).
- draft-from-slide-texts.ts: LAYOUT_TYPE_TO_CANVAS/FUNCTION + buildBlocksForLayout для всех 10 новых типов.
- fit-pass.ts: maxBlocks для каждого нового layout, contrastSafe для новых цветов.
- TypeCheck: 0 ошибок. ESLint: 0 ошибок. Build: успешен.

**Статус слоя** Идти можно

**Что стало внятнее** Все задачи из WEEK-2-SLIDE-TYPES.md выполнены. Каталог layout-ов вырос с 9 до 19, палитра с 4 до 8 цветов, шаблоны с 3 до 5.

**Что ещё мутно** Unsplash работает только при наличии UNSPLASH_ACCESS_KEY в .env.local (без ключа возвращает null, UI не ломается). Визуальная верификация каждого layout × template × color в браузере ещё не проводилась.

**CSS-рефакторинг** globals.css (~5000 строк) разбит на 12 модулей в `app/styles/`:
tokens · shell · start · chat · editor · slide-canvas-templates · slide-layouts · thumbnails · responsive · draft-screen · presenter · a11y.
globals.css стал индексным файлом из 12 `@import`-ов. Build + lint чисто.

**Передача смены** Запустить dev-сервер и визуально проверить новые layout-ы в редакторе. Попробовать генерацию через AI с новыми типами. Добавить UNSPLASH_ACCESS_KEY если нужны живые фото.

**Проблемы**
- Нет.

---

### 050 MVP Неделя 1 — Архитектура

**Что уложено** Все 6 задач WEEK-1-ARCHITECTURE.md выполнены:
- 1.1: Убраны SixSlotTuple, SlideSlotId, фиксированный SlideId. Типы переведены на массивы и string.
- 1.2: demo-generator.ts переработан — estimateSlideCount, buildDefaultSlidePlan, динамические массивы, addSlide/removeSlide/moveSlide.
- 1.3: AI-промпты обновлены (6–20 слайдов, layoutType), валидация 4–25, max_completion_tokens=4000, draft-from-slide-texts адаптирован.
- 1.4: В редакторе — кнопки ↑↓ перестановки, × удаления, + добавления слайда в rail.
- 1.5: localStorage автосохранение (debounce 1с, до 5 черновиков). На стартовом экране — список «Недавние черновики» с восстановлением и удалением.
- 1.6: Сетка draft screen переведена на auto-fill + overflow-y: auto, работает с любым количеством слайдов.

Тесты: 29/29, lint + typecheck + build чистые.

**Статус слоя** Внятно

**Что стало внятнее** Приложение больше не привязано к 6 слайдам. AI выбирает количество (6–20), пользователь может добавлять/удалять/переставлять. Черновик сохраняется в localStorage.

**Что ещё мутно** Drag-and-drop для перестановки — на MVP кнопок достаточно. Undo для удаления — нет, план предусматривал без него.

**Передача смены** Можно переходить к неделе 2 (WEEK-2-SLIDE-TYPES.md).

**Проблемы**
- TypeScript-ошибки каскадом после смены типов (ожидаемо) — починены файл за файлом через verify.

---

### 049 Защита переполнения

**Что уложено**
- Разобрана защита от переполнения на двух уровнях: весь слайд и отдельные блоки.
- Опора по коду: `presentation-types.ts` (`FitPassResult`, `DraftFitPassStrength`, `PresentationDraft.fitPassStrength`), `demo-generator.ts` (`buildPresentationDraft`, `runFitPassOnDraft`, `updateDraftAppearance`, `regenerateSlide`, `FIT_PASS_LIMITS`, `fitSlide`), `draft-from-slide-texts.ts` (`buildDraftFromSlideTexts`, `blocksFromSlideTextEntry`), `slide-canvas.tsx` (рендер и debug-слой), `globals.css` (контейнеры, `overflow`, `min-width: 0`, `minmax(0, 1fr)`), `draft-flow.test.ts`.
- Главный вывод: в проекте нет DOM-измерения переполнения слайда или блока через `scrollHeight/clientHeight/getBoundingClientRect`. Защита предрендерная и эвристическая: контент заранее ужимается, число блоков ограничивается, а CSS помогает безопасно уложить уже подготовленный текст.
- На уровне слайда центральный механизм — `fitSlide`: clamp заголовка, подзаголовка, `railTitle`, заголовков и тел блоков, labels действий; ограничение числа блоков по layout; расчёт флагов риска `overflowWidthRisk` и `overflowHeightRisk`; запись результата в `draft.debug.fitPassResultBySlide`.
- На уровне блока защита идёт тем же fit-pass: у блока нет собственного «авто-ремонта» по DOM. Для блоков действуют лимиты `blockTitle`, `blockBody`, `blockBodyPlaceholder`, плюс `maxBlocks` по layout. В CSS дополнительно включены `min-width: 0`, сетки `minmax(0, 1fr)`, переносы слов и локальные `overflow`/скролл только у редакторских и чатовых контейнеров.
- Для входа в редактор используется мягкий режим `editor`: `buildDraftFromSlideTexts` повторно прогоняет `runFitPassOnDraft` с `fitPassStrength: "editor"`, поэтому живые тексты из модели режутся заметно слабее, чем демо-черновик в `strict`.

**Статус слоя** Внятно

**Что стало внятнее** Стало ясно, что защита переполнения в проекте — это не «измерить канву и потом ужать DOM», а единый fit-pass над данными с двумя профилями жёсткости (`strict` и `editor`) плюс контейнерные CSS-ограничения для редактора и чата.

**Что ещё мутно** Если позже понадобится защита по реальной высоте/ширине пикселей, это будет уже следующий слой: отдельный DOM-fit pass или layout-measure phase. В текущем проекте такого механизма нет.

**Передача смены** Для повторения механики смотреть: `demo-generator.ts` (лимиты и fit-pass), `draft-from-slide-texts.ts` (editor-вход), `presentation-prototype.tsx` (точки вызова build/regenerate/update appearance), `slide-canvas.tsx` (только рендер + debug), `globals.css` (контейнерные страховки от локального overflow), `draft-flow.test.ts` (проверка `editor`-режима).

**Проблемы**
- Нет.

---

### 048 Первая генерация слайдов

**Что уложено**
- Причина: `buildDraftFromSlideTexts` подменял блоки через `buildBlocksForLayout`, который знает только metrics/steps/personas/features/checklist; для `cover`, `stat-focus`, `quote`, `comparison` срабатывал общий fallback (три «карточки»), несовместимый с вёрсткой `SlideCanvas`. Смена шаблона вызывала `updateDraftAppearance` → полный `buildPresentationDraft` из `blockPlan`, поэтому визуал «чинился».
- Исправление: в `draft-from-slide-texts.ts` функция `blocksFromSlideTextEntry` для перечисленных layout’ов сохраняет структуру блоков из плана и подставляет тексты из `slideTexts`; остальные layout’ы как раньше через `buildBlocksForLayout`.
- `updateDraftAppearance` в `demo-generator.ts`: только обновление `templateId`/`colorThemeId` в `workingDraft` + `runFitPassOnDraft`, без пересборки слайдов с нуля (тексты модели не затираются при смене шаблона/цвета).
- Дополнение (пропажа/укорочение текста): `fitSlide` для демо жёстко clamp’ил заголовки блоков до 34 и body до 156 символов — тексты модели превращались в «…». После смены шаблона раньше пересборка подставляла короткие демо-строки и выглядело «нормально». Сейчас: у `PresentationDraft` поле `fitPassStrength` (`strict` | `editor`), `buildDraftFromSlideTexts` выставляет `editor` и мягкие лимиты; `regenerateSlide` и `buildPresentationDraft` пробрасывают `fitPassStrength` с черновика.
- Тесты: обложка + длинная строка на `features` без обрезки; `npm run verify` — зелёный.

**Статус слоя** Внятно

**Что стало внятнее** Первый заход в редактор: и структура блоков под layout, и полнота текста с модели; смена шаблона не откатывает контент и не «чинит» за счёт подмены коротких строк.

**Что ещё мутно** При `editor`-лимитах вёрстка может переполняться — смотреть по реальным слайдам; эвристики quote/comparison/stat-focus по буллетам можно уточнить.

**Передача смены** `draft-from-slide-texts.ts` (`blocksFromSlideTextEntry`, `fitPassStrength: editor`), `demo-generator.ts` (`FIT_PASS_LIMITS`, `fitSlide`, `runFitPassOnDraft`, `updateDraftAppearance`, `regenerateSlide` + опции `buildPresentationDraft`), `presentation-types.ts` (`DraftFitPassStrength`, `fitPassStrength`).

**Проблемы**
- Нет.

---

### 047 Токенизация font-size globals

**Что уложено**
- В `presentations-frontend/app/globals.css` все `font-size`, совпадающие со шкалой `:root` (`--text-xs` … `--text-lg`), заменены на `var(--text-*)` без смены визуала; `--text-xl` в правилах не встречался (кроме определения переменной).
- Остальные объявления `font-size` (clamp, дробные rem вне шкалы) оставлены как были и помечены комментарием `/* нестандартный размер */`.
- `npm run verify` в `presentations-frontend` — зелёный.

**Статус слоя** Внятно

**Что стало внятнее** Шкала типографики из записи 030 реально используется в глобальных стилях; проще искать отклонения от ступеней.

**Что ещё мутно** Много нестандартных размеров на слайдах и в хроме — следующий шаг при желании: свести к шкале или расширить токены отдельной сменой.

**Передача смены** Искать `font-size:` и `var(--text-` в `globals.css`; нестандартные — по комментарию в конце строки.

**Проблемы**
- Нет.

---

### 046 Rail редактора — мини-превью

**Что уложено**
- Новый компонент `editor-slide-rail-thumb.tsx`: превью 120×68px (16:9), `data-template` + `data-color` как у canvas, верхняя полоска-акцент, номер слайда, одна строка `slide.title` (fallback на подпись rail как у старого thumbnail), `aria-label` по номеру и rail.
- `editor-screen.tsx`: rail на `EditorSlideRailThumb` + `templateId`/`colorThemeId` из `workingDraft`. `slide-canvas.tsx` не менялся; экспорт `SlideThumbnail` оставлен.
- `globals.css`: блок стилей превью (градиенты зеркалят `.slide-canvas` для strict/cards и briefing), активная рамка `var(--accent-primary)`, hover рамки, focus-visible; на ≤720px у `.editor-rail` добавлен `display: flex` для горизонтального скролла + `.rail-list` в ряд.
- Smoke `.ux-audit/ux-runner.cjs`: счётчик миниатюр по `.editor-rail-thumb`.

**Статус слоя** Внятно

**Что стало внятнее** В rail видно цвет темы и заголовок слайда без чтения длинной подписи rail; активный слайд выделен синей рамкой.

**Что ещё мутно** Превью не отражает 11 layout-типов (намеренно упрощение). Дублируются правила фона с `.slide-canvas` — при смене палитры canvas править оба места или вынести токены отдельной сменой.

**Передача смены** Превью rail: `editor-slide-rail-thumb.tsx` + секция «Editor rail: мини-превью» в `globals.css`. Smoke ищет `.editor-rail-thumb`.

**Проблемы**
- Нет.

---

### 045 Замечания ревью

**Что уложено**
- `/api/draft`: лимит размера тела (256 KiB), разбор JSON через `req.text()`, лимит запросов на инстанс (окно 60 с, 48 запросов/IP; на serverless не общий между репликами), безопасный ответ 500 без утечки внутреннего `Error.message`, структурный лог в stderr (`scope`, `event`, `code`/`internal`, `ts`).
- Коды ошибок: `PAYLOAD_TOO_LARGE` (413), `RATE_LIMIT_EXCEEDED` (429).
- ESLint: `eslint.config.mjs` на `eslint-config-next/core-web-vitals`, скрипт `lint`, `verify` = lint + typecheck + tests + build; правки под правила React 19 (`draft-screen` без setState в эффекте, `editor-screen` ref через `useLayoutEffect`).
- Тесты: `tests/draft-route-guards.test.ts`, `tests/draft-session.test.ts`.
- CI: deploy URL из `vercel deploy --format json` + `scripts/extract-vercel-deploy-url.mjs`.
- a11y: старт — `role="alert"` у ошибки, `role="status"` + `aria-live="polite"` у текста загрузки.
- Smoke: `.ux-audit/ux-runner.cjs` синхронизирован с копирайтом building-screen и `aria-label` кнопки настроек в editor.

**Статус слоя** Идти можно

**Что стало внятнее** Ревью переведено в конкретные ограждения API, проверку стиля и более устойчивый парсинг URL деплоя.

**Что ещё мутно** Жёсткий общий rate limit — только с внешним store (KV).

**Передача смены** При росте нагрузки — Upstash/Vercel KV для лимитов; при смене формата JSON CLI — проверить `extract-vercel-deploy-url.mjs`.

**Проблемы**
- Нет.

---

### 044 Подсказка под чипами старта

**Что уложено**
- Убрана строка подсказки «Тестовый поток: … Внятно …» на первом экране: удалено поле `description` у чипа «Показ продукта» в `presentation-options.ts`; тип чипов допускает опциональный `description` на будущее. Обработчики hover/focus в `start-screen` учитывают отсутствие текста.
- `aria-describedby` у чипов только если есть видимая подсказка (`helperText`), чтобы не ссылаться на отсутствующий `#start-scenario-hint`.

**Статус слоя** Идти можно

**Что стало внятнее** Под чипами больше не показывается внутренний тестовый копирайт.

**Что ещё мутно** Промпт по клику на чип (`PRODUCT_DEMO_PROMPT`) по-прежнему про демо продукта — менять только по отдельной задаче.

**Передача смены** При появлении новых чипов с подсказками снова заполнять `description`.

**Проблемы**
- Нет.

---

### 043 Экран уточнений черновика

**Что уложено**
- Экран черновика (`draft-screen`): убран верхний блок с резюме и чипами слева; сетка 6 карточек сразу на всю колонку.
- Смысловая сводка и чипы (аудитория, шаг, факты) перенесены в правую колонку в блок «Контекст презентации» под заголовком чата.
- «Открыть редактор» перенесена в правую панель: одна строка с «Назад» слева и кнопкой справа (`draft-screen__chat-toolbar`).
- Вёрстка без прокрутки всей страницы: сетка 2×3 с `minmax(0,1fr)`, у карточек прокрутка только у списка тезисов при переполнении; у колонки чата — `overflow-x: hidden`, у пузырей — `overflow-x: auto` для длинных строк; `overscroll-behavior: contain`.
- Адаптив: при ≤1100px сетка 2×3 строки, при ≤640px одна колонка ×6 строк; удалены стили `draft-screen__build-bar` / привязка к старому `draft-screen__summary` в медиазапросах.
- Smoke `ux-runner.cjs`: селекторы сводки и счётчика сообщений обновлены под новую разметку.

**Статус слоя** Идти можно

**Что стало внятнее** Слева только слайды, справа чат + контекст + переход в редактор; меньше двойных скроллов.

**Что ещё мутно** На очень низкой высоте окна 6 карточек остаются плотными; узкий чат — перенос тулбара по `flex-wrap`.

**Передача смены** Прогнать `ux-runner` при необходимости; экран clarify (до черновика) не менялся.

**Проблемы**
- Нет.

---

### 042 QA + фронтенд-ревью

**Что уложено**
- P0 a11y: `<ArrowLeft>` в кнопке «К черновику» получил `aria-hidden="true"` — все остальные иконки редактора уже имели; регрессия устранена.
- P1: Draft chat send кнопка получила `type="button"` и `aria-label="Отправить сообщение"`.
- P1 layout: `draft-screen__chat-messages` переведён с `flex: 1` (= `1 1 0%`) на `flex: 1 1 auto` + `min-height: 0` — стабильное поведение в flex-контейнере.
- P1 UX: Пустой bullet text теперь показывает плейсхолдер «Тезис» через `.slide-text-card__empty`, вместо пустого клик-региона без обратной связи.
- P1 a11y: Skeleton-карточки слайдов получили `aria-busy="true"` и `aria-label="Слайд NN загружается"`; контент скелетона помечен `aria-hidden="true"`.
- P1 a11y: Bullet span получили `aria-label="Тезис N: текст"` — было только у title/subtitle.
- CSS: Мёртвые классы `draft-screen__msg`, `draft-screen__msg--*`, `draft-screen__msg-bubble*`, `draft-screen__dot`, `@keyframes draft-dot-bounce` удалены (~50 строк) — были заменены унифицированными `chat-msg*` в предыдущей смене.
- CSS: `.build-status` переведён с `display: grid` на `display: flex; flex-direction: column; align-items: center` — спиннер центрируется без `margin: auto`.
- CSS: `editor-topbar-secondary` border-right `var(--border-soft)` → `var(--border-strong)` — разделитель вторичных/первичных действий стал заметен.
- CSS: `.draft-screen__chat-empty` dashed border убран, заменён `background: var(--bg-card-soft)` — меньше визуального шума.

**Статус слоя** Внятно

**Что стало внятнее** Регрессия aria-hidden закрыта. Skeleton карточки стали понятны для screen reader. Bullet поля теперь имеют плейсхолдер и правильный aria-label. CSS похудел на 50 строк мёртвого кода.

**Что ещё мутно** P2 технический долг из аудита 039 (превью шаблонов в селекторах, `buildMissingFacts`) остаётся в очереди.

**Передача смены** QA пройден, все найденные проблемы закрыты. TypeScript и linter чистые.

**Проблемы**
- Нет.

---

### 041 UX-чистка интерфейса

**Что уложено**
- Системный фикс курсора: `cursor: text` → `cursor: pointer` на всех полях SlideTextCard (title, subtitle, bullet). Поля теперь визуально предсказуемы как кнопки.
- Единая система `:focus-visible` для всего UI: 16 классов кнопок и интерактивных элементов получили кольцо `2px solid var(--accent-primary)`. Клавиатурная навигация стала рабочей.
- Фикс hover-трансформов при disabled: все кнопки (`.ghost-button`, `.primary-button`, `.chat-cta`, `.chat-compose-send`, `.composer-send`) теперь используют `:hover:not(:disabled)`.
- Добавлены недостающие `disabled`-состояния: `.chat-compose-send:disabled`, `.chat-suggestion-chip:disabled`, `.ghost-button:disabled`, `.composer-send:disabled`.
- Устранён дубль `.build-status__lead` — удалена старая декларация на строке ~737, остался единственный стиль рядом со спиннером.
- `prefers-reduced-motion`: анимации `.chat-dot`, `.build-status__spinner`, `.slide-text-card__skeleton-line` выключаются при предпочтении пользователя.
- Визуальный разделитель между вторичными действиями и первичным CTA в топбаре редактора (border-right). На экранах < 720px убирается.
- `draft-screen__chat-messages`: удалён лишний класс `chat-messages` (каскадная зависимость), добавлен `aria-live="polite"`.
- `clarify-screen` chat-messages: добавлен `aria-live="polite"`.
- Draft build-кнопка: добавлены `type="button"` и `aria-label`.
- Пустое состояние в чат-панели черновика: когда сообщений нет, показывается инструкция-подсказка.
- Overflow-защита для `draft-screen__summary-chip` (max-width + white-space:nowrap + text-overflow:ellipsis).
- `ghost-button:disabled` добавлено рядом с `primary-button:disabled`.

**Статус слоя** Внятно

**Что стало внятнее** Интерфейс стал консистентен по курсорам, состояниям hover/focus/disabled, анимациям и семантике доступности. Тема `:focus-visible` теперь покрывает весь поток.

**Что ещё мутно** P2-задачи из аудита 039 (превью шаблонов в селекторах, `buildMissingFacts`) по-прежнему в техдолге.

**Передача смены** CSS-слой состояний закрыт. Следующий шаг — E2E-тест или превью шаблонов.

**Проблемы**
- Нет.

---

### 040 Самостоятельное ревью

**Что уложено**
- Самостоятельный обзор репозитория (root workspace, `presentations-frontend`, CI, smoke, API, тесты) без изменений кода.
- В ответе пользователю: кто выигрывает, какая рабочая задача, приоритизированные улучшения (безопасность API, тесты, линтинг, разрезание тяжёлых модулей, CI, a11y, документация/продукт).

**Статус слоя** Идти можно

**Что стало внятнее** Карта сильных сторон и зон следующего усиления без смешения с отладкой конкретного бага.

**Что ещё мутно** Выбор приоритетов за продуктом/командой; отдельные смены на внедрение.

**Передача смены** Взять 1–2 пункта из списка улучшений и закрыть отдельными чатами с проверкой `verify`/`smoke`.

**Проблемы**
- Нет.

---

### 040 UI-примитивы BackButton ComposeField

**Что уложено**
- Создан `components/ui/back-button.tsx`: единая кнопка «Назад» с иконкой ArrowLeft. При наличии `label` — ghost-button (pill с текстом, clarify). Без `label` — `editor-back-button` (компактный квадрат, draft/editor).
- Создан `components/ui/compose-field.tsx`: единое поле ввода + кнопка отправки, три варианта: `start` (composer-box--start-v3, ArrowUp), `chat` (chat-compose-area + «Отправить», clarify), `draft` (только input+button без враппера, draft). Содержит обработчик Enter/Shift+Enter. Поддерживает `textareaRef` для фокуса при возврате на старт.
- В `globals.css` добавлен базовый класс `.compose-send` с общими свойствами кнопок отправки (background accent, white, border none, border-radius pill, cursor pointer, disabled/hover). Класс включён в transition-список и focus-visible-список.
- Заменены все три реализации в `clarify-screen.tsx`, `draft-screen.tsx`, `editor-screen.tsx`, `start-screen.tsx`. Убраны дублирующиеся `handleKeyDown` из clarify и draft.
- `eslint .` — 0 ошибок.

**Статус слоя** Внятно

**Что стало внятнее** Логика клавиатурного Enter, вид кнопки «Назад» и структура поля ввода теперь в одном месте. Три экрана не дублируют одну и ту же вёрстку.

**Что ещё мутно** CSS-классы `.chat-compose-send` и `.draft-screen__chat-send` сохранены как контекстные модификаторы поверх `.compose-send` — их можно объединить, если визуал впоследствии выровняют по токенам.

**Передача смены** Новые кнопки «Назад» — через `BackButton`, новые поля ввода — через `ComposeField`. При добавлении экрана с аналогичными паттернами — тот же компонент.

**Проблемы**
- Нет.

---

### 039 Аудит продукта

**Что уложено**
- Полный сквозной UX-аудит прототипа по всем слоям: UX, дизайн-система, копирайт, код, документация.
- Устранено 5 P0-проблем: fake loading в Clarify заменён нейтральными точками; Building screen упрощён до честного спиннера + summary без симуляции прогресса; Editor получил `handleBackToDraft` — путь назад к черновику без сброса сессии; SlideTextCard стал клавиатурно-доступным (`tabIndex`, `role="button"`, Enter/Escape); чат-компонент унифицирован между Clarify и Draft (одни классы, одни аватары, одна плотность).
- Устранено 7 P1-проблем: кнопка Sparkles получила текстовый лейбл «Настройки»; topbar редактора разделён на первичную (Показать) и вторичную (Шаблон, Цвет, Настройки) группы; Escape в SlideTextCard больше не сохраняет правку; дублирующие CSS-токены (`--radius-lg`, `--blue-cta`, `--text-muted`) приведены к алиасам; eyebrow «Что держим в фокусе» убран; добавлены `.chat-bubble--thinking`, `.chat-dot`, `.build-status__spinner`, `.editor-topbar-secondary`, `.editor-pill-btn--primary`.
- Документы синхронизированы: COPY_RULES.md — таблица бюджетов обновлена (Уточнение, Сборка, Черновик вместо Понимание/План); DESIGN_DECISIONS.md — зафиксировано отсутствие Pattern Selection в прототипе и осознанное добавление лейбла к Sparkles.

**Статус слоя** Внятно

**Что стало внятнее** Продукт стал честнее (нет симуляции AI и прогресса), целостнее (единый визуальный язык чата) и доступнее (клавиатура в SlideTextCard). Из редактора теперь есть путь назад без потери работы.

**Что ещё мутно** P2-задачи (превью шаблонов в селекторах, убрать footer nav на десктопе, починить `buildMissingFacts` в prompt-analysis) отложены как технический долг.

**Передача смены** P0 и P1 закрыты. P2 — следующая очередь.

**Проблемы**
- Нет.

---

### 038 Мини-чат draft

**Что уложено**
- Маршрут прототипа переведён на `start -> clarify -> building -> draft -> editor`.
- Введён единый `DraftSession`: один контракт для уточнения, сборки, черновика и входа в editor.
- `/api/draft` разделён на `clarify / generate / revise`; OpenAI-вызовы, таймауты и разбор ответа вынесены в отдельный `draft-adapter`.
- Убрана реконструкция `dummyWorkingDraft`: editor теперь собирается из того же `workingDraft`, что и clarify/draft, а ручные правки карточек переходят в editor без потери.
- Старт приведён к одному рабочему chip `Показ продукта`; уточнение стало необязательным по глубине; сборка вынесена в отдельный экран без пустого draft.
- В текущем чате стартовый chip выровнен по левому краю поля, а бренд возвращён к обычному написанию `Внятно` без латинской `t` в коде, smoke и текущих опорных документах.
- Убраны остаточные слабые места нового флоу: сервисные ошибки больше не попадают в продуктовую переписку для модели, back из `clarify` и `draft` не сносит контекст, а `building` больше не притворяется точным прогрессом.
- `DraftSession` теперь точнее синхронизируется со слайдами после ручных правок: summary, аудитория, missing facts и quick replies пересобираются из текущего набора карточек, а быстрые ответы стали нейтральными вместо самореференции про сервис.
- Открытие editor из draft защищено от битого контракта: при ошибке показывается inline-error вместо падения, кнопка не зависает, а smoke-мок теперь несёт полный `workingDraft` до editor.
- Обновлены `README.md`, `DESIGN_SPINE.md`, `SCREEN_ATLAS.md` под новый слой истины.
- Проверка пройдена: `npm run verify`, `npm run smoke`.

**Статус слоя** Идти можно

**Что стало внятнее**
- Первый путь теперь честный: ошибка первого запроса остаётся на старте, дальше есть отдельные состояния уточнения и сборки.
- Один и тот же смысловой контекст проходит в слайды и затем в editor.
- Smoke теперь проверяет именно новый путь, включая first-error на `clarify`, отдельный `building`, безопасный back, перенос правки из draft в editor и реальное открытие editor без падения на неполном контракте.

**Что ещё мутно**
- `clarify` сейчас детерминированный и опирается на локальную логику, а не на отдельную LLM-стратегию уточнения.
- `revise` обновляет `workingDraft` через склейку `sourcePrompt`, так что следующая отдельная проработка AI-слоя ещё нужна.

**Передача смены**
- Следующий слой: отдельно проработать стратегию AI-уточнения и сделать обновление `workingDraft` при draft-правках ещё точнее.
- Не возвращать концепт с латинской `t`, пока не будет отдельного явного решения по бренду.
- При изменениях маршрута держать в синхроне код, `README.md`, `DESIGN_SPINE.md`, `SCREEN_ATLAS.md` и smoke.

**Проблемы**
- `start -> clarify` раньше при `500` уводил пользователя в пустой draft -> переведено на inline-error на старте и отдельный building-screen.
- `draft -> editor` раньше терял смысловой контекст из-за `dummyWorkingDraft` -> editor теперь строится из реального `DraftSession`.
- Smoke раньше падал на `draft -> editor` из-за неполного mock `workingDraft` и маскировал реальный статус слоя -> мок доведён до полного контракта, а UI теперь даёт контролируемую ошибку вместо page crash.

### 037 Запуск localhost для просмотра

**Что уложено**
- След открыт. Запуск dev-сервера для локального просмотра.

**Статус слоя** Идти можно

**Что стало внятнее** Зафиксированы номер и короткое имя чата.

**Что ещё мутно** После просмотра — при необходимости обновить итог записи.

**Передача смены** Следующая смена: продолжить с `presentations-frontend` и маршрутом из `DESIGN_SPINE.md`.

**Проблемы**
- Нет.

### 036 Бренд Вняtно и латинская t

**Что уложено**
- Закреплено: вордмарк строго **Вняtно** (латинская `t` = точка опоры); в UI — `BrandWordmark` + `.brand-wordmark__t` (акцент `accent-primary`), `aria-label` «Внятно».
- Документы: `COPY_RULES.md` (раздел вордмарка), `DESIGN_DECISIONS.md` § v3 принципы, `SCREEN_ATLAS.md` схемы шапки.
- Код: `brand-mark.tsx`, `editor-screen.tsx`, `presentation-prototype.tsx`, `globals.css`.

**Статус слоя** Идти можно

**Что стало внятнее** Одно место в коде для названия рядом с маркой; правило для агентов и дизайна; Figma — перенести вручную по `DESIGN_DECISIONS`.

**Что ещё мутно** Canvas в Figma v3, если там ещё полностью кириллическое слово без акцента на t.

**Передача смены** Новые вордмарки — только через `BrandWordmark`.

**Проблемы**
- Нет.

### 035 Кобальт и карточки по умолчанию

**Что уложено**
- В `demo-generator.ts` значения по умолчанию: шаблон `cards` («Карточки»), цвет `indigo` («Кобальт»).

**Статус слоя** Идти можно

**Что стало внятнее** Новый черновик без явного выбора сразу в кобальте и шаблоне карточек.

**Что ещё мутно** Если где-то ещё зашиты старые дефолты вне `buildWorkingDraft` / `buildPresentationDraft` — не проверялись отдельно.

**Передача смены** При смене продуктового дефолта — править те же константы.

**Проблемы**
- Нет.

### 034 Актуализация AGENTS

**Что уложено**
- Убраны ссылки на удалённый HTML-референс `references/redesign-2026-04/preview.html` из `AGENTS.md`, `DESIGN_SPINE.md`, `SCREEN_ATLAS.md`, `UI_KIT_V0.md`.
- Закреплена актуальная опора: Figma v3 + `DESIGN_DECISIONS.md` § v3 → spine / UI-kit / atlas / copy rules.
- `SCREEN_ATLAS.md` переписан под v3 и факт кода: 5 вайрфреймов (Старт idle, Старт мини-чат, Черновик, Редактор closed, Редактор open), актуальный маршрут `start → draft → editor`, визуальные аннотации по v3, расхождение с Figma v3 (нет Pattern Selection, есть draft-screen) зафиксировано явно.
- `DESIGN_SPINE.md` — маршрут и роли экранов приведены к фактическому состоянию (убраны старые «Понимание задачи», «План», «Уточнение фактуры», «Экспорт»).

**Статус слоя** Идти можно

**Что стало внятнее** Агент читает атлас и сразу понимает текущий маршрут, визуальный язык и расхождения Figma vs код.

**Что ещё мутно** `DESIGN_SPINE.md` ниже обновлённых секций содержит разделы «Принципы дизайна v1» с пунктом «Тёмный редактор» — противоречие с v3, но не мешает работе агента (раздел помечен как мотивировка). При необходимости убрать.

**Передача смены** Документация актуализирована: `SCREEN_ATLAS`, `UI_KIT_V0`, `DESIGN_SPINE`, `AGENTS` — все под v3 и факт кода. Следующее по документации — разве что пункт «Тёмный редактор» в spine, если будет мешать.

**Проблемы**
- Нет.

### 033 Figma v3 иконки kit

**Что уложено** След открыт. Идёт синхронизация марки и иконки AI в Figma v3 Brand Kit с продуктом (`brand-mark.tsx`, `Sparkles` в редакторе).

**Статус слоя** Идти можно

**Что стало внятнее** Зафиксированы номер и короткое имя чата.

**Что ещё мутно** Правки в Figma после обхода файла.

**Передача смены** После правок — кратко обновить эту запись.

**Проблемы**
- Нет.

### 032 CSS v3 spacing

**Что уложено** В `globals.css` введены токены v3 Brand Kit: `--space-1`…`--space-9` (шаг 4px, кратность 8), `--radius-xs` (10px), `--radius-pill` (капсулы), `--radius-bubble-main` / `--radius-bubble-tail` для бабблов. Основные отступы оболочки, старта, чат-карточки (шапка/лента/compose/build), редактора (shell, topbar, rail, canvas, footer, drawer), слайда (`slide-canvas` — радиус 24px по § v3, паддинги через токены), блоков контента слайда и debug-слоя переведены на эти переменные; марка — `--radius-sm`. Кнопки в чате (compose, CTA, suggestion) — капсулы (`--radius-pill`); на старте белое поле — фиксированное скругление `--radius-start-field` (16px), кнопка отправки — вытянутая капсула (`--radius-pill`), снизу справа при многострочном поле (`align-self: flex-end`); чипы сценариев — `--radius-sm`, отступ ряда `margin-top: var(--space-7)`; ghost/primary — `--radius-pill`. Медиа ≤720px синхронизированы. `npm run verify` зелёный.

**Статус слоя** Идти можно

**Что стало внятнее** Один слой чисел из `DESIGN_DECISIONS.md` (v3): сетка, 20/24px на карточки, капсулы на chips/CTA/отправку; мелкий квадратный хром редактора — `--radius-xs`.

**Что ещё мутно** Шаблоны слайдов (`strict` / `cards` / `briefing`) по-прежнему с собственными радиусами (4/12/18px); body вне старта с лёгким градиентом — в v3 сцена плоская, но это не трогали, кроме уже существующего `app-shell--start`.

**Передача смены** Если в Figma v3 поменяют инсеты карточки чата или rail/drawer — править токены в `:root` и при необходимости `DESIGN_DECISIONS.md`.

**Проблемы**
- Нет.

### 031 Старт как в v3

**Что уложено** Экран старта приведён к макету v3 (Brand Kit 01): без подзаголовка под заголовком; поле ввода — белая капсула 600px (`max-width: 37.5rem`), тень `0 4px 12px`, одна строка по умолчанию (`rows={1}`, `field-sizing: content`); синяя кнопка отправки справа — вытянутая капсула (см. `--radius-pill` в `globals.css`); плейсхолдер «Нужно собрать питч для инвесторов…»; чипы сценариев — фон `#F1F3F9`, текст `#6B778C`, `gap` 12px; на старте `app-shell--start` даёт ровный фон `neutral.50` без градиентов body. Файлы: `start-screen.tsx`, `presentation-prototype.tsx`, `globals.css`. `npm run verify` зелёный.

**Статус слоя** Идти можно

**Что стало внятнее** Старт визуально совпадает с референсом v3 и с бюджетом текста в `COPY_RULES` для старта (без лишнего абзаца).

**Что ещё мутно** В углу остаётся марк + «Внятно» как в продукте; на присланном скрине этого нет — в Figma v3 блок обычно есть.

**Передача смены** Если в Figma поменяют плейсхолдер или чипы — синхронно `start-screen` / `SCENARIO_CHIPS`.

**Проблемы**
- Нет.

### 030 Три цикла критик

**Что уложено** Три цикла «критик → проверяющий → разработчик» по `presentations-frontend`. Цикл 1: удалён мусор `components/_test_heredoc.txt`; кнопка Sparkles в редакторе — `aria-label`/`title` «Открыть/Закрыть настройки слайда» в логике с заголовком drawer. Цикл 2: блок отладки слайда (`NEXT_PUBLIC_VNYATNO_DEBUG`) — русские подписи и aria, строки `formatTransforms`/`formatFitPassTitle` без англ. хвостов. Цикл 3: `IBM_Plex_Mono` — подмножество `cyrillic` для рендера кириллицы моноширинным слоем. `npm run verify` зелёный.

**Статус слоя** Идти можно

**Что стало внятнее** Меньше шума в дереве компонентов; согласованы подсказки кнопки панели с заголовком «Настройки слайда»; отладка читаема на русском; шрифты согласованы с `lang="ru"`.

**Что ещё мутно** Копирайт «Внятно» в UI совпадает с референсом редизайна; формальное пересечение с запретом слова «внятно» в `COPY_RULES.md` не трогали — приоритет у референса и продукта.

**Передача смены** При смене имени продукта в референсе — синхронно старт/редактор; при ужесточении `COPY_RULES` — отдельное продуктовое решение.

**Проблемы**
- Нет.

### 029 Внедрение плана Figma v3

**Что уложено** Дожато по чек-листу плана: старт — подзаголовок под «О чём презентация?»; чат — карточка 760px (`47.5rem`), время у сообщений (`createMessage`), счётчик вопросов только в `.sr-only`; сборка — шаги и подписи слайдов как в Figma, строка «N из 6 готово», статусы шагов, подзаголовок в шапке карточки; редактор — футер «Слайд N из M» со стрелками, заметки к слайду в drawer с привязкой к `slideSpeakerNotes`; марк — буква «В» в градиентном квадрате; `SCREEN_ATLAS` — явно зафиксировано отсутствие отдельного экрана паттерна в прототипе. Триггер панели — Sparkles; rail/topbar/drawer в стиле v3 (ранее в этой смене).

**Статус слоя** Идти можно

**Что стало внятнее** Прототип ближе к макету v3 по входу, чату, сборке и редактору; док расходится с Figma только там, где осознанно упрощён маршрут (паттерн).

**Что ещё мутно** Нет загрузки документа на старте (в макете была строка — без фиктивной кнопки не делали). Экраны 07–10 из Figma Screens по-прежнему вне прототипа.

**Передача смены** При смене копирайта сборки — держать согласованность с `BUILD_SLIDES_READY_BY_STEP` и четырьмя шагами.

**Проблемы**
- Нет.

### 028 AGENTS субагенты по усмотрению

**Что уложено** В `AGENTS.md` в разделе про сабагентов зафиксировано: агент может запускать их по своему усмотрению при явной пользе; порог «три и более трека» остаётся типичным автоматическим случаем.

**Статус слоя** Идти можно

**Что стало внятнее** Правила делегирования не выглядят как жёсткий триггер только при трёх треках.

**Что ещё мутно** Нет.

**Передача смены** При следующем уточнении порогов — править тот же раздел в `AGENTS.md`.

**Проблемы**
- Нет.

### 027 Figma v3 shell

**Что уложено** Оболочка приближена к v3 Brand Kit без новых экранов Pattern. `components/brand-mark.tsx`: единый SVG-марк; Start и `editor-screen` используют `<BrandMark />`, дублирование `start-logo__mark` убрано из CSS. Чат-карточка: `--chat-card-max-width: 43.75rem` (700px), `--radius-chat-card: 20px`. Редактор: rail `10rem` (160px), drawer `17.5rem` (280px). Фаза сборки: внутри той же `chat-card` — заголовок, синий progress bar, 4 шага, список из 6 слайдов с плейсхолдерами; таймеры 450/900/1350 ms + финиш 2200 ms; сессия удерживается в `buildSessionRef` до сборки черновика. `npm run verify` зелёный.

**Статус слоя** Идти можно

**Что стало внятнее** Один источник марки; чат и редактор ближе к числам из `DESIGN_DECISIONS.md` v3; Building визуально богаче и согласован с макетом по структуре.

**Что ещё мутно** Подписи шагов и слайдов в сборке не выверялись пиксель-в-пиксель с Figma; миниатюры на узком rail стоит проверить глазами на широком экране.

**Передача смены** Не возвращать дубли SVG марки; при смене таймингов сборки держать согласованность с 4 шагами и ref сессии.

**Проблемы**
- Нет.
  
### 001 Журнал следов

**Что уложено** Создан `SLED_SMEN.md` как единый журнал; задана структура записи с нумерацией, коротким именем чата и блоком проблем; правила в `AGENTS.md`, `VNYATNO.md` и `README.md` переведены на запись следа в файл вместо обязательного блока в чате; добавлено правило, что короткое имя и черновой след агент пишет первым действием.

**Статус слоя** Идти можно

**Что стало внятнее** Появилось одно место, где хранится итог по каждому рабочему чату; агентам задано, когда брать номер, как сразу фиксировать короткое имя вида `001 Журнал следов`, как обновлять только свою запись и когда смотреть прошлые проблемы перед долгим расследованием.

**Что ещё мутно** Не проверено на нескольких последовательных чатах вживую; если позже всплывёт неудобство с длиной коротких имён или со слишком ранним созданием черновых следов, правило ещё можно дожать без смены общей логики.

**Передача смены** Дальше важно не вернуть в `AGENTS.md` дублирование следа в финальном ответе; следующий агент должен первым делом открыть `SLED_SMEN.md`, завести заголовок вроде `002 Короткое имя`, а этот чат при продолжении обновляет только `001 Журнал следов`.
 
**Проблемы**
- Нет.

### 002 Деплой main

**Что уложено** Репозиторий переведён на работу только через `main`; в `AGENTS.md` и `README.md` закреплены правила одной ветки, коммитов по явной команде пользователя и нумерации коммитов по номеру смены; старый SlideForge вынесен в `old/`, а текущий продукт оставлен как новый минимальный `Next.js`-прототип; в `presentations-frontend/vercel.json` отключён Git auto-deploy Vercel; добавлен workflow `.github/workflows/presentations-ci.yml`, который на каждый push в `main` делает `npm run verify`, затем `vercel deploy --prod` и после этого переводит `vnyatno.vercel.app` на свежий production deployment; сделаны и запушены коммиты `002.1 feat: lock main deploy flow` и `002.2 chore: harden github actions runtime`.

**Статус слоя** Внятно

**Что стало внятнее** Теперь production-дорога одна и проверена на живом прогоне: push в `main` запускает workflow `Presentation Deploy`, `verify` и `deploy` проходят успешно, а `vercel inspect https://vnyatno.vercel.app` подтверждает, что домен смотрит на свежий production deployment `presentation-frontend-l22d51avr-mad1gds-4955s-projects.vercel.app`, созданный `2026-04-04 14:27:17` по Бангкоку.

**Что ещё мутно** В логах GitHub Actions остаётся предупреждение, что `actions/checkout@v4` и `actions/setup-node@v4` сами ещё таргетят старый runtime Node 20, хотя раннер уже принудительно переведён на Node 24; это не ломает деплой сейчас, но позже можно обновить major-версии actions, когда они появятся.

**Передача смены** Ближайший следующий шаг уже не про деплой, а про гигиену репозитория: по отдельной команде пользователя можно дочистить старые удалённые ветки, которые больше не нужны после перехода на `main only`; production-маршрут трогать не нужно, пока он продолжает идти через `.github/workflows/presentations-ci.yml` и `vnyatno.vercel.app`.

**Проблемы**
- `vnyatno.vercel.app` конфликтовал как domain project-level -> обошли это надёжным маршрутом через workflow: после каждого production deploy workflow явно ставит alias `vnyatno.vercel.app` на свежий deployment.

### 003 Экран запроса

**Что уложено** Первый экран маршрута `Запрос` переведён из hero + большой textarea в chat-first composer: шапка ужата до компактного состояния, в empty state оставлен акцент на активном шаге `Запрос`, остальные шаги собраны во вторичный compact rail; вместо hero добавлен рабочий intro block, ниже собран composer с textarea на 5 строк, встроенным CTA `Собрать рабочий черновик`, helper text и тремя кликабельными example cards; справа добавлен preview rail `Что получится` с тремя mock slide cards, чтобы above the fold сразу показывал реальный ход работы без пустой правой половины.

**Статус слоя** Идти можно

**Что стало внятнее** Первый шаг теперь выглядит как рабочий инструмент для квартального статуса, а не как промо-блок: сразу видно основной ввод, сильный CTA, примеры для старта и ожидаемый результат; композиция стала плотнее на десктопе и не ломает существующую тёмную сцену, токены и типографику.

**Что ещё мутно** Интерактивный прогон в штатном Playwright-профиле не состоялся из-за системного ограничения на профиль браузера; вместо этого экран проверен через `npm run typecheck`, `npm run build` и headless Chrome-скриншоты на десктопной и мобильной ширине.

**Передача смены** Если следующий слой пойдёт дальше по первому экрану, не возвращать hero и не раздувать старт обратно в витрину; ближайшее полезное продолжение — уже не layout стартового шага, а доведение перехода `Запрос -> Понимание` и живых состояний примеров/отправки.

**Проблемы**
- `mcp__playwright__browser_*` не смог открыть локальный экран из-за блокировки профиля (`EPERM` / browser profile in use) -> визуальную проверку сделали через headless Chrome со скриншотами `start-desktop.png` и `start-mobile.png`.

### 004 Первый выход

**Что уложено** Добавлен единый контракт `WorkingDraft` между первым выходом и сборкой; мок-парсер теперь сначала собирает `audience`, `period`, `goal`, `decisionNeeded`, `coreMessage`, `outline` и `openQuestions`, а уже потом из этого контракта строится черновик слайдов; после submit маршрут больше не прыгает в редактор, а проходит через реальные состояния `Понимание` и `План` с одним промежуточным экраном, где есть карточка `Понял задачу так`, chips, outline cards на 6 слайдов, действия `Поправить понимание` и `Продолжить сборку`, а при бездействии включается авто-продолжение через 1350 мс на каждый следующий шаг.

**Статус слоя** Идти можно

**Что стало внятнее** После экрана `Запрос` теперь видно не только, что система стартовала, но и что именно она поняла, какой план слайдов предлагает и какое решение ждёт от пользователя; правка понимания больше не возвращает в пустой экран, потому что исходный текст запроса сохраняется и открывается на доработку как есть.

**Что ещё мутно** Живой интерактивный прогон в Playwright снова упёрся в ограничение локального браузерного профиля и прав на `C:\WINDOWS\System32\.playwright-mcp`, поэтому пользовательский путь подтверждён через `npm run typecheck`, `npm run build`, локальный dev server и проверку отданного HTML, но не через полноценный браузерный клик-тест.

**Передача смены** Следующий полезный слой уже не про форму первого выхода, а про подключение реального backend-контракта вместо mock parser и про доведение живой браузерной проверки этого маршрута, когда получится обойти ограничение профиля Playwright.

**Проблемы**
- `mcp__playwright__browser_*` снова не смог открыть локальный маршрут из-за занятых/защищённых путей профиля (`browser in use`, затем `EPERM` на `C:\WINDOWS\System32\.playwright-mcp`) -> проверку закрыли через `npm run typecheck`, `npm run build`, локальный `npm run dev` и ответ `http://localhost:3000`.

### 005 Сборка между этапами

**Что уложено** Маршрут `Запрос -> Понимание -> План -> Сборка -> Черновик` доведён до более честного перехода: общий step rail стал компактным и единым на всех этапах, без отдельного стартового режима; `WorkingDraft` теперь хранит `sourcePrompt`, поэтому исходный запрос доезжает до сборки и редактора как часть одного объекта; экран `Сборка` перестроен из spinner-like состояния в двухколоночный экран с тремя короткими статусами `Фиксирую задачу / Собираю структуру / Заполняю черновик` и тремя live preview карточками будущих слайдов, которые сначала показывают заголовки, затем структуру и затем уже реальное содержимое и decision ask; в `Черновике` под header добавлен компактный meta block с исходным запросом и собранным решением, чтобы вход в редактор выглядел продолжением сборки, а не новым телепортированным режимом.

**Статус слоя** Идти можно

**Что стало внятнее** Между подтверждённым планом и открытием редактора больше нет пустой паузы; пользователь видит не объяснение процесса, а появление самих будущих слайдов из тех же данных, которые потом открываются в редакторе; исходный запрос и точка решения теперь остаются рядом с объектом и не теряются после перехода в `Черновик`.

**Что ещё мутно** Живой браузерный прогон на desktop и mobile в этой смене не делался; слой подтверждён через `npm run typecheck` и `npm run build`, но не через отдельный визуальный click-through тест в локальном браузере.

**Передача смены** Если следующий слой пойдёт дальше, не превращать `Черновик` в тяжёлую правую панель с настройками раньше времени: текущая опора — canvas-first редактор и компактный meta block сверху; ближайшее полезное продолжение уже не про loading-state, а про живую браузерную проверку desktop/mobile и про следующий отдельный слой доработки редактора.

**Проблемы**
- Нет.

### 006 Запуск сабагентов

**Что уложено** В `AGENTS.md` добавлен отдельный рабочий блок про делегирование и сабагентов; в нём закреплено, когда можно запускать параллельные треки без отдельного вопроса пользователю, какие типы задач для этого подходят, какие роли у главного треда, `explorer`, `worker` и опционального `reviewer`, когда ждать завершения всех сабагентов и что именно нужно собрать в одном итоговом своде.

**Статус слоя** Идти можно

**Что стало внятнее** Правило про сабагентов больше не висит отдельным англоязычным фрагментом и не спорит с остальным тоном проекта; стало понятно, что делегирование — это не витрина, а способ быстрее пройти многосоставную задачу, не потеряв сборку результата в главном треде.

**Что ещё мутно** Правило пока зафиксировано только в `AGENTS.md`; если позже в проекте появятся свои кастомные типы агентов или более жёсткие ограничения на делегирование, блок нужно будет дожать уже под реальный набор ролей.

**Передача смены** Дальше можно опираться на этот блок при задачах с тремя и более независимыми треками; если практика покажет, что порог, роли или формат свода неудобны, править лучше этот раздел точечно, а не разносить правила по разным файлам.

**Проблемы**
- Нет.

### 007 Опора редизайна

**Что уложено** Новый HTML-референс перенесён в `references/redesign-2026-04/preview.html` и закреплён рядом с `README.md` как доступная внутри репозитория опора для агентов; в `AGENTS.md`, `DESIGN_SPINE.md`, `SCREEN_ATLAS.md` и `UI_KIT_V0.md` добавлены явные пометки, что текущая рокировка временно опирается на этот референс и что подписи `Экран N` и `Скрытый слой` не являются пользовательским копирайтом; прототип в `presentations-frontend` перестроен под новый публичный маршрут `start -> clarify -> editor` с одним скрытым `fit-pass`, light-shell редактором, независимыми селекторами шаблона и цвета, drawer для локальной пересборки текущего слайда и новым 6-слайдовым draft без экранов `Понимание`, `План` и отдельной публичной `Сборки`; локальный прогон `npm run verify` проходит.

**Статус слоя** Идти можно

**Что стало внятнее** Теперь у проекта есть один референс редизайна внутри самого репозитория, а не по внешнему пути; старые дизайн-доки перестали молча спорить с новым каркасом; пользовательский путь снова короткий и соответствует референсу: тихий вход, короткое уточнение фокуса, затем сразу редактор; служебный `fit-pass` работает внутри генерации и не рендерится отдельным экраном; слой собран и проверен перед отправкой в production-дорогу.

**Что ещё мутно** `DESIGN_SPINE.md`, `SCREEN_ATLAS.md` и особенно `UI_KIT_V0.md` ещё не переписаны под новый референс целиком, а только подчинены ему пометками; живая браузерная click-through проверка снова не прошла через Playwright MCP из-за занятого профиля браузера, поэтому слой подтверждён через `npm run typecheck`, `npm run build`, поиск по UI-текстам и ручную проверку структуры кода, но не через полноценный автоматизированный клик-прогон.

**Передача смены** После отправки в `main` следующий полезный слой уже не про хранение референса, а про доведение оставшихся дизайн-доков до нового каркаса и про живую браузерную проверку маршрута в обход конфликта профиля Playwright; при дальнейшей работе не возвращать в UI stepper, отдельные review-экраны и самообъясняющие тексты из HTML-референса.

**Проблемы**
- `mcp__playwright__browser_navigate` снова упёрся в занятый профиль `ms-playwright` (`Browser is already in use`) -> живую браузерную проверку пришлось закрыть сборкой, типами и текстовым поиском по UI-копирайту.

### 008 Тихий старт

**Что уложено** Стартовый flow переписан под тихий вход без лишней объяснялки: на первом экране остались только бренд, вопрос, пустое многострочное поле, тихие example chips и `Продолжить`; после клика на том же экране раскрывается живой MiniClarificationChat с текстовыми ответами и внутренним confidence-порогом, который быстро останавливает разговор и открывает путь к `Собрать черновик`; код разрезан на координатор, стартовый composer, MiniClarificationChat, редактор и отдельные helper-файлы для prompt-аналитики и логики прояснения, а скрытые режимы разговора убраны из UI в внутренние `progress / structure / choice`; в финальном дожиме стартовому полю возвращён короткий placeholder `Что нужно показать?`.

**Статус слоя** Идти можно

**Что стало внятнее** Первый экран снова читается как один вход без смешения сценариев и режимов; скрытая классификация разговора убрана из UI и живёт только во внутренней логике; draft теперь строится из собранной ясности по аудитории, главному выводу, следующему шагу и фактуре, а не из двух ручных choice-chip ответов; структура кода стала передаваемой следующей смене без возврата к одному 400+ строчному контейнеру; стартовый текст на поле дожат до короткой рабочей формулировки без AI-интонации; свежий стартовый экран дополнительно снят через `next start -p 3002` и headless Edge в `test-results/start-screen-fresh.png`.

**Что ещё мутно** Живой click-through через Playwright MCP снова не удалось прогнать из-за занятого профиля браузера, поэтому слой подтверждён через `npm run verify`, текстовый поиск по UI-копирайту, production-like `next start` на `3002` и headless Edge-скрин стартового экрана, но без полноценного автоматизированного сценария с кликом по `Продолжить`.

**Передача смены** Дальше уже не возвращать на старт подзаголовки, helper-тексты и режимные chips; если следующий слой продолжит входной маршрут, опираться на текущий split по файлам (`prompt-analysis`, `clarification-flow`, `start-screen`, `mini-clarification-chat`, `editor-screen`) и добирать только реальные пробелы в эвристике или редакторе.

**Проблемы**
- `mcp__playwright__browser_*` снова упёрся в занятый профиль браузера -> визуальную проверку пришлось закрывать сборкой, текстовым поиском, `next start -p 3002` и headless Edge-скриншотом вместо нормального click-through.

### 009 Quiet shell
_Дополнение 2026-04-05:_ локальный слой дочищен до конца: `editor-screen` переведён на живые `drawerActions`, `clarification-flow` и `presentation-types` доведены до рабочей схемы после старого type-drift, `npm run verify` снова зелёный; quiet shell дополнительно прогнан на свежем `next start --port 3012` через headless Playwright на desktop и mobile, а `https://vnyatno.vercel.app` сверён как live baseline со старым редактором.

**Что уложено** Основной редактор перестроен в quiet working shell: после мини-чата путь идёт прямо в editor без отдельного full-screen шага; в `presentations-frontend/components/presentation-prototype.tsx` убран hidden build-stage с таймером, а draft теперь собирается сразу с внутренним `fit-pass`; в `presentations-frontend/lib/presentation-types.ts` и `presentations-frontend/lib/demo-generator.ts` скрытый 6-слотовый каркас разведён с публичным UI-слоем через `railTitle`, `railRhythm`, `drawerActions` и семантический `kind` у блоков; в `presentations-frontend/components/prototype/editor-screen.tsx`, `presentations-frontend/components/slide-canvas.tsx` и `presentations-frontend/app/globals.css` собран тихий top bar с редактируемым названием, отдельными dropdown `Шаблон` и `Цвет`, compact rail на 6 карточек, светлый офисный canvas и закрытый по умолчанию drawer с тремя локальными действиями без раскрытия внутренних режимов; `npm run verify` проходит.

**Статус слоя** Идти можно

**Что стало внятнее** Редактор теперь читается как собранный рабочий документ, а не как промежуточный конструктор: служебные названия слотов больше не нужны UI, rail держится на живых заголовках из содержания, шаблон и цвет разведены как независимые оси, а правая панель даёт только локальные правки активного слайда.

**Что ещё мутно** Из этой смены изменения ещё не уехали на `vnyatno.vercel.app`, поэтому live-домен проверен только как baseline `200 OK`, а не как подтверждение нового shell; локальный автоматизированный click-through в главном треде снова упёрся в занятый профиль Playwright MCP, так что маршрут подтверждён сборкой, локальным `next start --port 3011`, HTML-дампом и headless Edge-скрином стартового экрана, но не полноценным авто-кликом до editor.

**Передача смены** Дальше важно не вернуть `Новый запрос` и другие лишние элементы в тихую шапку редактора, не вытаскивать наружу служебный канон из 6 слотов и не превращать drawer в историю диалога; ближайший полезный следующий шаг уже не про локальную модель, а про живой click-through нового shell после отправки изменений по production-дороге.

**Проблемы**
- `mcp__playwright__browser_*` в главном треде снова упёрся в занятый профиль браузера (`Browser is already in use`) -> локальную браузерную проверку пришлось закрывать через `npm run verify`, `next start --port 3011`, HTML-дамп и headless Edge-скрин вместо полного авто-клика до editor.

### 010 Надёжный draft

**Что уложено** Генеративный слой перестроен вокруг одного скрытого `WorkingDraft`: в `presentations-frontend/lib/presentation-types.ts` зафиксирован новый контракт с `presentationIntent`, `confidence`, `slidePlan[6]`, `visibleSlideTitles[6]`, `templateId` и `colorThemeId`; в `presentations-frontend/lib/prompt-analysis.ts` и `presentations-frontend/lib/clarification-flow.ts` mini-chat сокращён до skeleton-readiness по аудитории, цели, фактам и недостающим данным; в `presentations-frontend/lib/demo-generator.ts` собран новый pipeline `prompt + mini-chat -> WorkingDraft -> 6-slot plan -> visible slides -> fit-pass -> single repair`, локальная пересборка ограничена активным слайдом, а правые action labels генерируются из скрытых transform'ов без системных слов. `presentations-frontend/components/presentation-prototype.tsx`, `presentations-frontend/components/prototype/editor-screen.tsx`, `presentations-frontend/components/slide-canvas.tsx` и `presentations-frontend/app/globals.css` переведены на тихий editor-shell без `activeVariant`, без видимых `speakerNote`/`ask`, с максимум тремя блоками на слайд, с build-status `Собираем черновик -> Уточняем структуру -> Проверяем читаемость` и dev-only debug-слоем под `NEXT_PUBLIC_VNYATNO_DEBUG=1`; `references/redesign-2026-04/preview.html` синхронизирован с новым слоем и больше не описывает visible speaker-support.

**Статус слоя** Внятно

**Что стало внятнее** Из сырого prompt и короткого чата теперь собирается взрослый office-draft на 6 слайдов без фальшивых чисел и без пустых заголовков; недостающие данные остаются в рабочих placeholder'ах, а не тормозят сборку. Шаблон и цвет живут в модели draft как source of truth, локальная пересборка меняет только активный слайд, fit-pass и debug-пакет, а наружу выходят только собранные слайды, человеческие заголовки и короткие статусы.

**Что ещё мутно** Полный browser-MCP click-through до editor в этой смене не подтвердился: локальный `mcp__playwright__browser_navigate` упирается в `EPERM` при попытке создать `C:\Windows\System32\.playwright-mcp`, поэтому визуальная автоматизация среды сейчас ненадёжна сама по себе, а не из-за кода приложения.

**Передача смены** Следующий полезный шаг уже не про модель и не про layout, а про инфраструктуру проверки: либо починить путь данных для browser MCP, либо добавить отдельный устойчивый визуальный раннер. Если после живого прогона всплывут реальные overflow-кейсы, можно отдельно усилить fit-pass DOM-метриками, не возвращая наружу hidden vocabulary и не ломая локальную пересборку.

**Проблемы**
- `mcp__playwright__browser_navigate` в этой среде блокируется на `EPERM: operation not permitted, mkdir 'C:\\Windows\\System32\\.playwright-mcp'` -> функциональную проверку закрыли через `npm run verify`, `next start` и HTML-ответ живого маршрута вместо полного auto-click сценария.

### 011 Гига-перестройка

**Что уложено** Mini-chat, парсинг prompt и генерация draft пересобраны вокруг достаточности и реального содержания: `presentations-frontend/lib/prompt-analysis.ts`, `presentations-frontend/lib/clarification-flow.ts` и `presentations-frontend/lib/demo-generator.ts` теперь вытаскивают тему, адресата, цель, факты и риск без директивного мусора, строят шесть content-led заголовков, три живых drawer actions и один общий путь через normalizer + fit-pass после первичной сборки, `regenerateSlide()` и смены внешнего вида; quiet shell дожат копирайтом в `presentations-frontend/components/presentation-prototype.tsx`, `presentations-frontend/components/prototype/editor-screen.tsx` и `presentations-frontend/components/slide-canvas.tsx`, а локальный smoke и regression-путь синхронизированы через `.ux-audit/ux-audit.spec.cjs` и `README.md`.

**Статус слоя** Идти можно

**Что стало внятнее** Mini-chat больше не крутит ритуальный summary и спрашивает только недостающее: богатые запросы идут сразу в черновик, а бедные добирают ровно аудиторию или 1-2 факта; rail titles перестали брать в лоб `Собери...` и `Нужно показать...`, а benchmark про Vnyatno уже собирается как разговор о продукте с собственными опорами (`MVP`, `тихий старт`, `сборка черновика`, `редактор`, `локальная пересборка`); drawer перестал звучать как команда машине, build-status стал тише, а единый fit-pass держит clamp заголовков, placeholder’ы и локальную пересборку без разъезда hidden draft и visible UI.

**Что ещё мутно** Полноценный auto-click через Playwright MCP в этой среде снова не состоялся из-за занятого браузерного профиля, поэтому живой локальный проход закрыт комбинацией regression-скрипта, `npm run typecheck`, `npm run build`, `npm run verify`, production-like `next start --port 3100` и HTML-ответом; в `next dev` остались шумные transient module-resolution ошибки от старого локального инстанса, хотя production build и `next start` уже чисто поднимают актуальный shell.

**Передача смены** Следующий полезный слой уже не про draft-логику, а про реальный продовый smoke после push в `main`: перепроверить `https://vnyatno.vercel.app` на новом билде, убедиться, что rail/drawer/build-flow совпали с локальным слоем, и только потом идти в следующий круг polish.

**Проблемы**
- `mcp__playwright__browser_*` снова упёрся в занятый профиль `ms-playwright` (`Browser is already in use`) -> локальную браузерную проверку пришлось закрыть через regression-скрипты, `next start` и HTML-снятие живого маршрута вместо нормального auto-click.

### 012 UI Design v1 в Figma

**Что уложено** Создан Figma-файл «Вняtно — UI Design v1» (https://www.figma.com/design/CXBGVOHmhpond41SwSwxTf) с тремя страницами: 6 экранов пользовательского маршрута (Start → Chat → Pattern Selection → Building → Editor → Editor+Drawer), 6 демо-слайдов о продукте и мини-стайлгайд (палитра + типографика). Добавлен новый экран Pattern Selection между чатом и сборкой. Чат переделан в полноценный интерфейс с аватарками и бабблами. Создан `DESIGN_DECISIONS.md` с описанием всех решений. Работа велась в ветке `design/figma-screens-apr2026`.

**Статус слоя** Идти можно

**Что стало внятнее** Появился единый визуальный маршрут от входа до редактора, который можно показать и обсуждать. Pattern Selection закрывает пробел между чатом и сборкой — даёт пользователю ощущение контроля. Демо-презентация о продукте собрана по COPY_RULES без внутреннего словаря.

**Что ещё мутно** Экраны статичные — нет интерактивных прототипов и переходов. Стайлгайд содержит только цвета и типографику, компоненты не вынесены. Демо-слайды не привязаны к реальной генерации — это ручной дизайн, а не вывод системы.

**Передача смены** Следующий слой: (1) собрать интерактивный прототип в Figma с переходами между экранами, (2) перенести дизайн-решения в код — начать с чата и Pattern Selection, (3) решить, нужен ли мобильный вид.

**Проблемы**
- Figma MCP на Starter-плане имеет лимит 6 вызовов/месяц. Для работы потребовался апгрейд до Professional ($16/мес).

### 013 Продолжение дизайна Figma

**Что уложено** Figma-файл (https://www.figma.com/design/CXBGVOHmhpond41SwSwxTf) доведён до единого сценария: все 10 экранов связаны питч-историей Вняtно для инвесторов. Починен сломанный экран Pattern Selection (03) — карточки пересобраны с правильной высотой, контентом и визуальной структурой. Обновлён Chat (02) — диалог теперь про создание питча. Обновлены Editor (05) и Editor+Drawer (06) — слайд «Три шага до черновика» с тремя шаговыми карточками вместо generic-метрик. Building (04) переведён на narrative «Собираем питч». Start (01), Slideshow (07), Mobile (10) — плейсхолдеры и контент синхронизированы. Обновлён `DESIGN_DECISIONS.md`.

**Статус слоя** Идти можно

**Что стало внятнее** Появился единый сквозной сценарий: пользователь приходит → описывает задачу (питч для инвесторов) → чат уточняет → выбирает паттерн «Питч» → система собирает → открывается редактор со слайдом «Три шага до черновика» → справа drawer с AI-действиями. Демо-слайды на Page 2 уже были про Вняtно — теперь и UI-экраны согласованы.

**Что ещё мутно** Экраны статичные — нет интерактивных прототипов с переходами. Стайлгайд на Page 3 содержит только палитру и шрифты, компоненты не вынесены. Template Variants (08) остаётся скелетным. На слайде CTA (demo slide 06) сломано изображение QR-кода (checkerboard).

**Передача смены** Следующий полезный слой: (1) перенести дизайн-решения из Figma в код, начиная с чата и Pattern Selection; (2) собрать интерактивный прототип в Figma с переходами; (3) решить, нужен ли мобильный вид для MVP.

**Проблемы**
- Pattern Selection (03) имел height=10 у карточек (баг из чата 012) → пересобрали карточки с нуля через use_figma.

### 014 Дизайн-ревизия и Figma v2

**Что уложено** Figma-файл (CXBGVOHmhpond41SwSwxTf), страница **v2 — Redesign**. Два прохода: первый — сборка всех экранов и слайдов; второй — аудит и перетряска.

Экраны (6 шт), все перестроены во втором проходе:
- 01 — Start: светлый градиент, центрированный ввод, чипы сценариев, подсказка про загрузку документа
- 02 — Chat: широкая карточка 720px, чистые бабблы без таймстампов, аватарки, suggestion chips, индикатор «Вопрос 2 из 3», зелёная CTA «Собрать черновик →»
- 03 — Pattern Selection: три карточки с нумерованными шагами и коннекторами, примеры, бейдж «Рекомендуем», опция «Пропустить»
- 04 — Building: простая карточка — заголовок, прогресс-бар, 4 текстовые строки, 6 названий слайдов. Без анимаций и таймеров.
- 05 — Editor: rail + canvas + toggle-кнопка drawer на правом краю
- 06 — Editor + Drawer: открытая правая панель с AI-действиями

Демо-слайды (6 шт) — питч Вняtно — без изменений во втором проходе:
- 01 Обложка · 02 Проблема · 03 Три шага · 04 Результат · 05 Для кого · 06 След. шаг

**Статус слоя** Идти можно
**Что стало внятнее** Все экраны прошли аудит и стали чище. Chat убран мусор (таймстампы), расширен. Pattern Selection — нумерованные шаги вместо wireframe-миниатюр. Building упрощён до MVP-минимума. Editor получил toggle drawer.
**Что ещё мутно** Экраны статичные (нет прототипа). Компоненты не в библиотеке. Мобильной версии нет.
**Передача смены** Следующий слой: перенести дизайн в код.
**Проблемы**
- SOLID fills в Figma API не принимают alpha в color — нужно использовать opacity на уровне fill.
- layoutSizingHorizontal='FILL' работает только после appendChild в auto-layout parent.

### 015 Figma v2 в код

**Что уложено** Стартовый экран и чат перенесены из Figma v2 — Redesign (файл CXBGVOHmhpond41SwSwxTf, страница 50:2) в код. Шрифт Inter заменил IBM Plex Sans / Rubik в `app/layout.tsx`. CSS-токены и стили переписаны в `globals.css`: новые переменные (`--bubble-user`, `--bubble-bot`, `--green-cta`, `--shadow-chat-card`), обновлённые радиусы (24px для карточек), стили стартового экрана (центрированная композиция, белая карточка ввода с кнопкой-стрелкой, чипы сценариев, подсказка про документ), стили чата (белая карточка 720px, аватарки с инициалами, бабблы user/bot с разными радиусами, зелёная CTA «Собрать черновик», compose-зона с кнопкой-стрелкой, индикатор «Вопрос N из 3»). Логотип вынесен в фиксированный блок `.start-logo` в `presentation-prototype.tsx`. `npm run verify` зелёный, оба экрана проверены в браузере через cursor-ide-browser.

**Статус слоя** Идти можно

**Что стало внятнее** Start и Chat теперь визуально совпадают с Figma v2: светлый градиентный фон, центрированный вход, белая карточка чата с аватарками, зелёная CTA, compose-зона. Шрифт единый (Inter). Логика clarification-flow и генерации не тронута.

**Что ещё мутно** Экраны Pattern Selection, Building, Editor и Editor+Drawer из v2 ещё не перенесены.

**Передача смены** Следующий слой: перенос экранов 03-06 из Figma v2 (Pattern Selection, Building, Editor, Editor+Drawer). Не возвращать IBM Plex Sans, не возвращать старый формат шапки app-topbar, не превращать чат в тяжёлый диалог с историей.

**Проблемы**
- Figma MCP get_metadata не позволяет получить список страниц файла — пришлось искать node ID страницы v2 вручную (50:2).
- Другой dev-сервер на порту 3000 мешал запуску нового — решено через taskkill.

### 016 Ревизионный проход Start+Chat

**Что уложено** Второй проход по стартовому экрану и чату. Suggestion chips стали интерактивными: клик по чипу сразу отправляет ответ и переходит к следующему вопросу. Три набора chips привязаны к `pendingSlot` из clarification-flow (audience → Инвесторам/Клиентам/Команде, desiredOutcome → Согласовать бюджет/Показать прогресс/Получить решение, knownFacts → MVP готов/Есть первые метрики/Пилот запущен). CSS: добавлены transitions на `.composer-send`, `.chat-cta`, `.chat-compose-send`, `.chat-suggestion-chip`; `min-height: 32rem` для `.chat-card`; почищены media queries от мёртвых классов (`.entry-card`, `.chat-compose-box`, `.chat-ready`); добавлен responsive для `.chat-messages`, `.chat-card-header`, `.chat-card-sep`, `.chat-compose-area`; градиент и тень на лого-марку `.start-logo__mark`; disabled-стиль composer через CSS-класс вместо inline style.

**Статус слоя** Идти можно

**Что стало внятнее** Чат теперь полностью интерактивный: chips кликабельны, flow проходится без ручного набора текста. Визуальная точность Figma ↔ код повышена: градиент на марке, transitions, min-height карточки.

**Что ещё мутно** Экраны 03-06 из v2 не перенесены. Вопросы бота не включают варианты ответов в текст (в Figma: «Кому показываем — инвесторам, клиентам или команде?», в коде: «Кому показываем?» + chips). Это различие в логике clarification-flow, не в дизайне.

**Передача смены** Следующий слой: перенос экранов 03-06 или правка текстов clarification-flow для большей разговорности.

**Проблемы**
- `handleSubmitReply` читал `chatReply` из замыкания, а не свежее значение. Исправлено добавлением `directValue?: string` параметра.

### 017 UX-аудит большой

**Что уложено** Полный click-through прогон Start → Chat → Build → Editor на живом dev-сервере через cursor-ide-browser (desktop 1280×800 + mobile 390×844). Собран структурированный аудит: 6 категорий, 30+ конкретных проблем с серьёзностью. Зафиксирован приоритетный план из 10 пунктов.

**Статус слоя** Внятно

**Что стало внятнее** Теперь есть единый список проблем по всем экранам: hydration error, сломанный регистр «mVP», некликабельный drawer trigger, обещание загрузки документа, отсутствие Pattern Selection и footer-навигации, нативные select вместо segmented controls, невидимый build-transition (440ms). Расхождения с Figma v2 зафиксированы в отдельной таблице.

**Что ещё мутно** Аудит проведён визуально и по коду, но не включает accessibility-проверку (contrast ratio, screen reader). Мобильный прогон через редактор не прошёл до конца — нужна ручная проверка на реальном устройстве.

**Передача смены** Следующий полезный шаг — взять любой пункт из приоритетного плана (начиная с hydration error, mVP-регистра и drawer trigger) и довести до рабочего состояния. Не расширять аудит без фиксации найденного.

**Проблемы**
- Hydration error в `start-screen.tsx` (строка 30) — `children` prop меняет рендер `<h2>` между сервером и клиентом.
- Drawer trigger `writing-mode: vertical-rl` — браузер не может к нему проскроллить, клик не работает.

### 018 Редизайн лого

**Что уложено** Переделан логотип: вместо синего градиентного квадрата с буквой «В» — тёмная марка (`#0f0f11`) с инлайн-SVG из трёх горизонтальных линий убывающей длины (иконка структуры слайда). Изменены: `presentation-prototype.tsx` (JSX марки), `globals.css` (`.start-logo__mark` — убран градиент, цвет, шрифт; добавлен тёмный фон и тонкая inset-тень).

**Статус слоя** Идти можно

### 019 Фикс UX-аудита

**Что уложено** Исправлены 10 проблем из приоритетного плана аудита 017:
1. Hydration error — убран условный рендер `<h2>` через `children`, теперь заголовок внутри единого branch.
2. Регистр «mVP» — `topicInPhrase` теперь не трогает слова целиком в uppercase (MVP, API, KPI).
3. Drawer trigger — убран `transform: rotate(180deg)`, `align-self: center`, фиксированная высота.
4. «Или загрузи документ» — удалена строка, placeholder заменён на рабочий.
5. Кнопка «Назад» — «←» в editor-brand, `handleBackToStart` (полный reset → start).
6. Build-transition — задержка 800/1800ms вместо 220/440ms.
7. Segmented controls — нативные `<select>` заменены, лейблы на русском.
8. Prompt fragments — усилена фильтрация директивных фраз.
9. Compose-зона — скрывается при `readyToBuild`.
10. Мобильный layout — rail горизонтальный, drawer-trigger скрыт, segmented оборачивается.

**Статус слоя** Внятно

**Что стало внятнее** Все 10 проблем из аудита 017 закрыты. TypeScript без ошибок.

**Что ещё мутно** Pattern Selection (экран 3 из Figma) не реализован. Accessibility-проверка не проводилась.

**Передача смены** Проверить визуально на dev-сервере. Следующий крупный шаг — экран Pattern Selection.

**Проблемы**
- Нет.

### 020 Бренд-кит и агенты

**Что уложено** Зафиксирован в индексе агентских документов файл `AGENTS_BRAND_KIT_sovcom-inspired.md` (опциональный гайд тона и визуальной подачи, не канон продукта); в `AGENTS.md` добавлены правило приоритета относительно `DESIGN_SPINE` / `COPY_RULES` / референса редизайна, дополнение «стены» документ↔UI, указание что устаревшие Cursor rules лежат в `old/.cursor/rules/`; в `README.md` — короткий блок «Документы для агентов».

**Статус слоя** Идти можно

**Что стало внятнее** Агенты видят, где лежит бренд-кит и когда его открывать; меньше риска подменить им продуктовые дизайн-доки или втащить внутренний словарь на экран.

**Что ещё мутно** Сам бренд-кит длинный; при конфликте с `COPY_RULES` по-прежнему ведущий продуктовый слой.

**Передача смены** При смене стиля продукта обновлять связку дизайн-доков; бренд-кит трогать только если меняется договорённость про опциональный референс тона.

**Проблемы**
- Нет.

### 022 Насыщенные слайды

**Что уложено** Полная переработка визуала слайдов: 4 цветовые темы заменены на насыщенные (Графит #334155, Кобальт #4338CA, Изумруд #0D9488, Янтарь #D97706) с градиентными акцентными полосами; 3 шаблона стали визуально различимы (Строгий — 4px углы + левый акцент, Карточки — 18px + глубокие тени, Брифинг — 12px + обводка); 8 иконок заменены на стильные Lucide (Zap, BarChart3, TrendingUp, ShieldAlert, Target, Search, ArrowUpRight, Hourglass); иконки блоков увеличены до 2.5rem; типографика заголовков усилена (800 weight, clamp до 2.85rem); тона блоков насыщены (Tailwind-уровень: #C7D2FE, #A7F3D0, #FDE68A); ритм-полоски в rail-миниатюрах также стали ярче. Файлы: `slide-canvas.tsx`, `globals.css`, `demo-generator.ts`. `npm run verify` зелёный, экраны проверены через cursor-ide-browser.

**Статус слоя** Внятно

**Что стало внятнее** Каждая из 12 комбинаций (4 цвета × 3 шаблона) теперь даёт визуально отличимый результат: раньше все выглядели почти одинаково, теперь переключение темы и шаблона меняет характер слайда заметно. Иконки перестали быть мелкими невыразительными квадратиками и стали опорой блока.

**Что ещё мутно** Проверка была в viewport ~800px (стекшийся в мобильную раскладку); на десктопе >1200px с трёхколоночным layout нужна отдельная проверка.

**Передача смены** Не возвращать бледные пастельные цвета и одинаковые радиусы для всех шаблонов. Если следующий слой будет про десктопный layout, проверить, что акцентная полоса (::before) не обрезается border-radius на широком канвасе.

**Проблемы**
- После замены иконок остался fallback `?? Lightbulb` без импорта → исправлен на `?? Zap` в тот же проход.

### 021 Figma под бренд-кит

**Что уложено** В Figma-файле (CXBGVOHmhpond41SwSwxTf) создана страница **v3 — Brand Kit** с 6 экранами (Start, Chat, Pattern Selection, Building, Editor, Editor+Drawer), пересобранными по `AGENTS_BRAND_KIT_sovcom-inspired.md`: белая сцена #F7F9FC, синий акцент #2D4BEE, коралловый #F26161, мягкие тени, радиусы 20-24px, Inter. В `AGENTS.md` добавлен блок «Figma-файл проекта» с fileKey и ссылкой; `DESIGN_DECISIONS.md` дополнен описанием v3 с палитрой и принципами.

**Статус слоя** Идти можно

**Что стало внятнее** Появился визуальный референс, в котором все экраны собраны по одной палитре и одному набору принципов бренд-кита; стартовый экран переделан из первого кричащего варианта в тихий вход; агенты теперь видят Figma fileKey прямо в `AGENTS.md`.

**Что ещё мутно** Демо-слайды (Page 2) не пересобирались; токены не вынесены в Figma Variables (вшиты в fills напрямую).

**Передача смены** При переносе v3 в код опираться на палитру из `DESIGN_DECISIONS.md` (v3); если нужен Figma Variables layer — завести его отдельным шагом; не возвращать тёмный Start и зелёный CTA из v2.

**Проблемы**
- `use_figma` ошибка `TypeError: object is not extensible` при попытке задать `topRightCornerRadius` на bubble-фреймах → обошли единым `cornerRadius` без per-corner override.
- Первый вариант Start был слишком промо-кричащим (крупные метрики «90 сек / 6 слайдов / 0 ₽») → переделан в тихий вход по просьбе пользователя.
- Все 6 фреймов имели auto-layout с HUG по высоте вместо FIXED 900px (01=683, 02=520, 03=722, 04=785, 05=156, 06=156) → исправлено: 01-04 зафиксированы на 900px, 05-06 полностью пересобраны с `layoutSizingVertical = 'FIXED'` и правильной FILL-структурой MainArea.

### 024 Топбар редактора

**Что уложено** Полная переработка топбара редактора. Кнопка «Другой взгляд» → иконка Sparkles (круглая toggle-кнопка); body-сетка: 2 колонки по умолчанию, 3 при открытом drawer через `data-drawer`; кнопка назад `←` → Lucide `ArrowLeft`; бренд-марк `В` → SVG-барры с coral→blue градиентом (как на главной); убраны лейблы «ШАБЛОН» и «ЦВЕТ»; segmented-кнопки компактнее (padding 0.4/0.7); поле названия компактнее (font 0.9375rem, min-height 2.5rem); медиа-брейкпоинт сдвинут с 1200px на 960px — при средних экранах layout остаётся десктопным. Файлы: `editor-screen.tsx`, `globals.css`.

**Статус слоя** Внятно

**Что стало внятнее** Топбар стал единой аккуратной строкой: [← марка Внятно] [название] [шаблоны] [цвета] [✨]. Бренд-марк везде одинаковый. Лейблы не нужны — segmented-контролы сами себя объясняют. Иконка Sparkles понятна как «переосмыслить» без текста.

**Что ещё мутно** При очень длинных заголовках название обрезается (ellipsis), но хватает для типичных.

**Передача смены** Не возвращать текст «Другой взгляд», вертикальный таб и лейблы. Бренд-марк теперь одинаковый: SVG-барры + coral→blue; если менять — менять везде.

**Проблемы**
- Брейкпоинт 1200px ломал layout в cursor-ide-browser (viewport ~1024px) — сдвинут на 960px.

### 026 Визуальный аудит слайдов

**Что уложено** Визуальный аудит всех 6 layouts: найдено 8 проблем, все починены. Cover: subtitle теперь в uppercase над заголовком (CSS `order: -1`), контент вертикально отцентрирован, убрано пустое пространство. Metrics: regex `extractMetricValues` исправлен — ловит «200мс» вместо «200м» (добавлены единицы `мс|мин|сек|ч|дн`), убрано дублирование label/desc (desc теперь пустой). Steps: добавлен body текст через `buildStepBodies()` — показывает desiredOutcome, gap, audience. Personas fallback: вместо «[нужна цифра]» мусора — осмысленные карточки «Где упираемся / Что уже известно / Какое решение нужно» с реальными данными. Features: первый блок теперь «Следующий шаг» вместо дублирования title слайда. `findRiskLine` и `isRiskSignal` расширены на «не доказано». Subtitle-мусор на tension убран проверкой `isPlaceholderToken`. Глобальный `.slide-canvas__subtitle` отвязан от `text-transform: uppercase` (это осталось только у cover через override). Написана документация `SLIDE_COMPONENTS.md`. `npm run verify` зелёный. Визуальный прогон всех 6 слайдов на 3 шаблонах (Строгий, Карточки, Брифинг) и 2 темах (Графит, Кобальт).

**Статус слоя** Внятно

**Что стало внятнее** Каждый слайд визуально отличается от остальных и не содержит мусорных placeholder'ов. Cover — чистый титульник с subtitle как section-label. Metrics — крупные числа с корректными единицами. Steps — шаги с body текстом. Checklist — без изменений, уже был хорош. Personas — fallback для одного адресата стал осмысленным. Features — без дублей title.

**Что ещё мутно** Personas при одном адресате первая карточка «Где упираемся» пока placeholder — заработает лучше, когда промпт содержит явные риски. Steps body текст третьего шага иногда пустой. Desktop >1200px не проверялся.

**Передача смены** Не возвращать мусорные placeholder'ы в personas и subtitle. При доработке метрик можно улучшить `extractMetricValues` — парсить составные единицы точнее. Документация по компонентам лежит в `SLIDE_COMPONENTS.md`.

**Проблемы**
- Нет.

### 025 Компоненты из Figma

**Что уложено** Система слайдов переведена с одинаковых 3-блочных карточек на 6 разных визуальных компонентов по образцу демо-презентации из Figma v2: cover (крупный заголовок + мета), metrics (карточки с крупными числами), steps (нумерованные шаги 01/02/03 с коннекторами), checklist (строки с галочками), personas (карточки-персоны с ролью/задачей/тегом), features (иконка + заголовок + описание). В `presentation-types.ts` расширен `CanvasLayoutId` (6 новых значений) и `SlideBlock` (опциональные `metric`, `stepNumber`, `tagline`). В `demo-generator.ts` каждая slide function привязана к своему layout, `buildBlocks` разведён на 6 отдельных builder-функций, добавлен `extractMetricValues` для вытаскивания чисел из фактов. В `slide-canvas.tsx` вместо единого `blocks.map()` — switch по layout с отдельным JSX для каждого компонента; `SlideThumbnail` показывает мини-превью по типу layout. В `globals.css` — полный набор стилей для всех 6 компонентов с поддержкой шаблонов (strict/cards/briefing) и цветовых тем (slate/indigo/teal/sand). Исправлен баг `splitAudience`, который разрезал «Руководитель направления» по каждой букве «и». `npm run verify` зелёный, все 6 слайдов проверены визуально через cursor-ide-browser на двух промптах (бедный и богатый).

**Статус слоя** Внятно

**Что стало внятнее** Каждый из 6 слайдов теперь визуально отличается от остальных: cover — чистый титульник, metrics — крупные числа, steps — шаговая последовательность, checklist — список с галочками, personas — карточки-роли, features — фичи с иконками. При богатом промпте с цифрами (12%, 200мс, 40%) система вытаскивает числа в метрики автоматически. При бедном промпте показывает placeholder'ы.

**Что ещё мутно** Метки description в метриках иногда обрезаются неудобно (показывают «Конверсия выросла на» вместо полного текста). Personas при одном адресате (не перечислении) падают в fallback с placeholder'ами. Десктопная проверка >1200px не проводилась.

**Передача смены** Не возвращать одинаковые 3-блочные карточки. При доработке метрик можно улучшить `extractMetricValues` — парсить единицы измерения точнее. Personas полезно доработать: если адресат один, вместо fallback показывать другой layout (например, тот же checklist или features).

**Проблемы**
- `splitAudience` разрезал «Руководитель направления» по каждой кириллической «и» → исправлен regex на `\s+и\s+` (только союз «и» как отдельное слово).

### 023 Brand Kit в код

**Что уложено** Палитра сайта переведена с v2 (indigo #4568F6 + зелёный CTA #2E8C59) на v3 Brand Kit (#2D4BEE синий + #F26161 коралловый + #F7F9FC белая сцена). В `globals.css`: перемаппинг всех `:root` переменных на новые токены; body gradient заменён на мягкий синий; brand-mark и start-logo__mark получили новый градиент (лого — коралл→синий в стиле Совкомбанка); CTA чата переведён с зелёного на синий; аватар пользователя стал коралловым, бота — синим; 12 мест с hardcoded rgba(69,104,246) заменены на rgba(45,75,238); build-status, regen-button, separators и input focus приведены к новой палитре; мелкие хардкоды (#f0f2f5, #e8ebf0, #f3f6fa, #f9fafb) заменены на CSS-переменные. `npm run verify` зелёный; полный click-through Start → Chat → Editor проверен через cursor-ide-browser.

**Статус слоя** Внятно

**Что стало внятнее** Сайт теперь визуально совпадает с Figma v3 — Brand Kit: белая сцена, синий как опора, коралловый как тёплый акцент, мягкие тени; зелёный CTA полностью убран; лого обыгрывает двуцветную механику бренд-кита.

**Что ещё мутно** Темы слайдов (slate/indigo/teal/sand) не пересобирались — они про содержание слайда, не про оболочку. Desktop-width прогон в cursor-ide-browser показал мобильный layout (viewport уже 1200px); отдельная проверка на широком десктопе не состоялась.

**Передача смены** Не возвращать зелёный CTA, тёмный старт и старый indigo #4568F6. Если следующий слой пойдёт дальше по визуалу, можно доработать темы слайдов под brand kit и проверить desktop layout >1200px.

**Проблемы**
- Нет.

### 024 Слайды глубже

**Что уложено** Реализованы все 5 слоёв. `briefing` стал тёмным шаблоном — каждый из 4 цветов даёт тёмный фон с ярким акцентом. Добавлены 3 новых layout: `stat-focus` (один крупный показатель на 2/3 слайда), `quote` (сильное утверждение с подтверждением), `comparison` (два столбца до/после). Генератор теперь сам выбирает лучший layout по содержанию: `chooseBestLayout()` с `detectComparison()`. В чеклисте иконка выбирается по тону блока (constraint → AlertTriangle, movement → CheckCircle). Персоны получили иконку роли в шапке. Шаги — иконку рядом с номером. Словарь иконок расширен с 8 до 14 (`chart`, `people`, `star`, `warning`, `rocket`, `check`). В метриках добавлены delta-индикаторы (TrendingUp/Down по тону) и progress bar для процентных значений.

**Статус слоя** Внятно

**Что стало внятнее** Три шаблона теперь визуально разные: strict — светлый документный, cards — воздушные карточки, briefing — тёмный с акцентом. Слайды внутри одной презентации больше отличаются: stat-focus ломает сетку из трёх карточек, quote работает как риск-слайд без персон, comparison напрямую показывает до/после. Иконки несут смысловую нагрузку в checklist.

**Что ещё мутно** Thumbnails в rail не обновлены под новые layout'ы (stat-focus, quote, comparison показывают пустой превью). Desktop-width прогон не проводился. При `detectComparison` порог строгий — нужен primaryGap И desiredOutcome; при бедном промпте оба могут быть placeholder и система останется на checklist.

**Передача смены** Не возвращать единую иконку Check в checklist. Если пойдёт работа по thumbnails — добавить preview-шаблоны для stat-focus/quote/comparison в `renderThumbnailPreview()` (или аналог). Briefing + dark — не регрессировать к светлому.

**Проблемы**
- Нет.

### 025 Экран черновика со слайдами

**Что уложено** После первого промпта пользователь попадает на новый split-экран: слева сетка из 6 текстовых карточек слайдов (AI-генерация через GPT-4o-mini), справа чат без ограничений по сообщениям. Разговор с помощником обновляет тексты слайдов. Каждое поле слайда (заголовок, подзаголовок, буллеты) редактируется вручную по клику. Кнопка «Собрать черновик» всегда видна и ведёт в существующий editor. Добавлены: `SlideTextEntry`, `DraftChatMessage` в типах; `PrototypeScreen` расширен значением `"draft"`; API-маршрут `/api/draft` с двумя режимами (generate/chat); компоненты `DraftScreen`, `SlideTextCard`; конвертер `buildDraftFromSlideTexts` в demo-generator; CSS-слой для draft-экрана и карточек. Build зелёный, typecheck чистый.

**Статус слоя** Внятно

**Что стало внятнее** Пользователь видит результат (черновик слайдов) сразу после первого промпта, не проходя через вопросы уточнения. Если данных мало — AI заполняет слайды правдоподобным деловым содержанием. Чат и ручное редактирование работают параллельно.

**Что ещё мутно** На слайде 6 (шаги) крайняя правая карточка может слегка обрезаться при длинном тексте — minor, не блокирует. Streaming в /api/draft не реализован (batch-ответ), при медленном соединении пользователь видит скелетон до 10 сек.

**Передача смены** Не возвращать beginClarify как основной путь submit (заменён на beginDraft). При желании можно добавить streaming в /api/draft; слайд 6 (steps) — проверить обрезку при длинных заголовках шагов.

**Проблемы**
- Блоки создавались с `title: ""` и `body: bullet` — но checklist/features/steps рендерят основной текст из `b.title`, а не `b.body`. Исправлено: `title: bullet`, `body: ""`. Заодно убраны layouts `hero`/`split` для слотов 2 и 3 (заменены на `checklist`) — иначе дефолтный fallback рендерил бы оба поля и дублировал текст.
- `.env.local` лежал в корне репо, Next.js искал его в `presentations-frontend/` — скопировано на место.


### 026 Полноэкранный режим

**Что уложено** Добавлен полноэкранный режим показа презентации в редакторе. Кнопка «Показать» (Maximize2) появилась в топбаре рядом с Sparkles. При нажатии открывается `presenter-overlay` — тёмный экран со слайдом по центру (16:9, `min(92vw, 92vh*16/9)`), стрелки prev/next по бокам, счётчик «1 / 6» внизу, крестик выхода сверху справа. Параллельно запрашивается native Fullscreen API на `document.documentElement`. Клавиши: `←` / `↑` — назад, `→` / `↓` / `Space` — вперёд. Escape работает через браузерный `fullscreenchange` event. Все хуки (`useState`, `useEffect`, `useRef`) расположены до раннего `return null`. Стрелочная навигация использует `useRef`-паттерн (без stale closure). TypeScript чистый, lint чистый.

**Статус слоя** Внятно

**Что стало внятнее** Пользователь может показать готовую презентацию прямо из редактора, не покидая продукт.

**Что ещё мутно** На мобильных устройствах `requestFullscreen()` может требовать user-gesture — это стандартное ограничение браузеров, обходить не нужно. При очень узком вьюпорте кнопки prev/next могут перекрывать слайд — не критично для десктопного сценария.

**Передача смены** Кнопка называется «Показать», не «Презентация» и не «Fullscreen» — так короче и понятнее в русскоязычном UI.

### 027 Показ продукта — демо-флоу

**Что уложено** Исправлен маппинг layout'ов в `buildDraftFromSlideTexts`: слоты теперь идут cover → metrics → steps → checklist → personas → features (было cover → checklist → checklist → features → checklist → steps). Расширена функция `slideTextEntryToSlide` — добавлен `buildBlocksForLayout`, который парсит специфический формат контента под каждый layout: metrics — `"число — описание"` → `metric` + `title`; steps — `"01 Действие"` → `stepNumber` + `title`; personas — `"Роль — Задача"` → `title` + `tagline`/`body`; features — `type: decision, icon: arrow`; checklist — `type: movement, icon: check`. Обновлён `GENERATE_SYSTEM` в `/api/draft/route.ts`: структура переориентирована с «Контекст/Суть/Доказательство/Риски» на «Обложка/Проблема/Три шага/Результат/Для кого/Следующий шаг» с явными инструкциями по формату bullets под каждый слот. Добавлен сценарий-чип «Показ продукта» в `SCENARIO_CHIPS` и `EXAMPLE_PROMPTS` с клиентским промптом. Билд зелёный, TypeScript чистый.

**Статус слоя** Внятно

**Что стало внятнее** Нажать «Показ продукта» → AI генерирует 6 слайдов с правильными layout'ами: cover с заголовком, metrics с 3 числами, steps с 01/02/03, checklist с галочками, personas с 3 ролями, features с шагами CTA.

**Что ещё мутно** AI может не всегда точно выдержать формат `"число — описание"` в буллетах metrics — зависит от модели. Если число не найдено, метрика рендерится без значения (`"—"`). Thumbnails в rail для metrics/steps/personas не были изменены — они показывают упрощённый превью.

**Передача смены** Не возвращать старый CANVAS_LAYOUT_BY_SLOT со случайными checklist/steps. Если качество AI-генерации metrics недостаточное — можно добавить постпроцессинг на стороне route.ts: явно форматировать bullets слота 2 в нужный формат.

### 028 Старт — только чип «Показ продукта»

**Что уложено** На первом экране нижние сценарные чипсы визуально остаются, но кликабелен только «Показ продукта» (`pokaz`); остальные `disabled` + стили `example-chip:disabled`. Константа `START_SCREEN_ENABLED_SCENARIO_ID` в `demo-generator.ts`, используется в `start-screen.tsx`.

**Статус слоя** Внятно

**Что стало внятнее** Демо-флоу не уводит в три других готовых сценария случайным кликом.

**Что ещё мутно** Когда понадобятся все чипсы — снять блокировку или сделать флагом окружения.

**Передача смены** Искать `START_SCREEN_ENABLED_SCENARIO_ID` и `.example-chip:disabled`.

**Проблемы**
- Нет.

### 030 UX аудит — правки

**Что уложено**
- Building screen: 4 шага прогресса вместо голого спиннера (по SCREEN_ATLAS). Кнопка «Назад» возвращает на clarify.
- Start screen: иконка ArrowUp вместо символа `↑`; placeholder сокращён до 7 слов.
- Аватарки: бот → `BrandMark--sm` (тот же марк, что в хедере); пользователь `Вы` → `Я`.
- Draft screen: топбар с brand mark + стрелка «Назад»; CTA «Открыть редактор» в фиксированном футере; toolbar удалён.
- Clarify screen: «Собрать черновик» — primary только по `readyToGenerate`, ghost-кнопка до этого.
- Editor drawer: «Настройки» → «Доработка».
- CSS: токен `--accent-error`; удалены алиасы `--blue-cta`, `--text-muted`; мёртвые классы `.entry-subtitle`, `.chat-card-counter`, `.chat-compose-input--disabled` удалены; `BrandMarkSvg` удалён из `brand-mark.tsx`; hover у `example-chip` — только darkening, без синего.
- Editable-поля в SlideTextCard: `cursor: text` + dashed outline при hover — явная affordance.
- CSS type scale: 6 переменных (`--text-xs` … `--text-xl`) добавлены в `:root`.

**Статус слоя** Внятно

**Что стало внятнее**
- Экраны start/clarify/building/draft теперь в едином визуальном хроме: brand mark виден везде.
- Building screen перестал выглядеть как зависший экран — шаги показывают прогресс.
- Draft screen стал одним продуктом, а не двумя панелями без имени.

**Что ещё мутно**
- Slide rail в редакторе остаётся текстовым (без визуальных превью слайдов) — P2 из аудита, не тронут.
- Shared components (Button, ComposeField) не вынесены — дублирование CSS продолжается — P0 из аудита, требует отдельной смены.
- Точные совпадения со шкалой перенесены на `var(--text-*)` в записи 047; дробные rem и `clamp` остаются с пометкой `/* нестандартный размер */` в `globals.css`.

**Передача смены**
- Если трогать draft screen — опора: `draft-screen.tsx` + CSS секция `Draft screen`.
- Если трогать building — опора: `building-screen.tsx` + CSS `.build-status__step`.

**Проблемы**
- Нет.

### 029 Ревизия качества

**Что уложено**
- `/api/draft` сведен в один контракт: helper `draft-api.ts`, строгая валидация 6 слотов по `id`/`railTitle`, явные ошибки `INVALID_REQUEST` / `MODEL_INVALID_*` / `*_TIMEOUT`, chat payload без дубля последнего сообщения.
- Продуктовый путь закреплён как `start -> draft -> editor`: живой UI уже идёт так, mini-chat удалён из кода как мёртвый слой (`mini-clarification-chat.tsx`, `clarification-flow.ts`), типы и `presentation-prototype.tsx` упрощены под один маршрут.
- Поверхность поддержки уменьшена: `demo-generator.ts` разрезан, сценарии и draft-конвертер вынесены в `presentation-options.ts` и `draft-from-slide-texts.ts`.
- Локальная проверка проекта приведена к одному сценарию: `npm run smoke` теперь сам собирает build, мокает `/api/draft`, проходит `start -> draft -> editor`, открывает drawer и presenter mode, пишет `results.json`.
- CI усилен: после `verify` идёт отдельный `smoke` job с `npx playwright install --with-deps chromium`, только затем deploy.

**Статус слоя** Внятно

**Что стало внятнее**
- Код, README, smoke и CI теперь говорят об одном и том же маршруте.
- Draft API больше не может тихо переставить ответ модели по слотам или переусилить последнее пользовательское сообщение из-за дубля в payload.
- Проверки стали честными: unit-тесты реально запускаются в workspace, smoke реально проходит в браузере без OpenAI key.

**Что ещё мутно**
- В рабочем дереве остались отдельные чужие правки в документах (`COPY_RULES.md`, `DESIGN_DECISIONS.md`, `SCREEN_ATLAS.md`, `VNYATNO.md`), их в этом чате не сводили и не откатывали.

**Передача смены**
- Если дальше трогать draft-flow, опора теперь такая: `presentations-frontend/lib/draft-api.ts`, `presentations-frontend/app/api/draft/route.ts`, `npm run smoke`, `.github/workflows/presentations-ci.yml`.
- Если понадобится расширять smoke, держать один маршрут и один источник истины, не возвращать mini-chat скрытым обходом.

**Проблемы**
- `npm ci` не проходил: lockfile отстал от актуальных workspace-зависимостей -> синхронизировано через `npm install`, после чего `verify` и `smoke` стали воспроизводимыми.
- Windows-runner для smoke зависал после успешного прохода -> сервер переведён с `npm start` на прямой запуск `next` binary, добавлено корректное завершение дочернего процесса.

### 030 Смоук перед показом

**Что уложено**
- Полный verify чистый: lint, typecheck, 29 unit-тестов, production build.
- Ручной smoke-тест базового флоу в браузере: start → clarify → building → draft → editor → presenter → drawer. Всё проходит без багов.
- Hydration-предупреждение в dev — артефакт Cursor-браузера (`data-cursor-ref`), пользователи не увидят.
- CI smoke починен: `scripts/smoke-runner.cjs` в репозитории (раньше вызывался `.ux-audit/ux-runner.cjs`, которого нет на раннере), ожидания синхронизированы с UI (building, drawer «Доработка», назад «К уточнению»), артефакты smoke в `test-results/smoke/`.

**Статус слоя** Внятно

**Что стало внятнее**
- Подтверждено: весь маршрут start → draft → editor → presenter рабочий, можно показывать первым пользователям.
- GitHub Actions снова может дойти до job `deploy` и обновить production на Vercel.

**Что ещё мутно**
- Локальный `.ux-audit/ux-runner.cjs` у разработчика может расходиться с `scripts/smoke-runner.cjs`, если копировать правки вручную.

**Передача смены**
- Smoke для CI и локально: `npm run smoke` → `scripts/smoke-runner.cjs`. Деплой: workflow `Presentation Deploy`, alias `vnyatno.vercel.app`.

**Проблемы**
- Build конфликтовал с запущенным dev-сервером (Next.js lock) → убит порт 3000, rebuild прошёл чисто.
- Push 030.1: job `smoke` в CI падал (`MODULE_NOT_FOUND` на `.ux-audit/ux-runner.cjs`) → деплой не запускался; исправлено переносом и обновлением селекторов (030.2).

### 031 CSS-модули из globals

**Что уложено**
- Монолитный `globals.css` (~5000 строк) разрезан на 12 CSS-модулей в `app/styles/`: tokens, shell, start, chat, editor, slide-canvas-templates, slide-layouts, thumbnails, responsive, draft-screen, presenter, a11y.
- Содержимое перенесено один-к-одному, без правок и переформатирования.

**Статус слоя** Внятно

---

### 032 Неделя 4: PDF-экспорт + полировка

**Что уложено**
- **4.5a ErrorBoundary**: создан `components/ui/error-boundary.tsx`, `SlideCanvas` обёрнут во всех трёх местах (editor-main, presenter overlay, PdfRenderHost). Ошибка рендера слайда больше не крашит приложение.
- **4.5b Retry + стабильность**: `callOpenAI` в `draft-adapter.ts` обёрнут в `callOpenAIWithRetry` — при 502/504 до 2 повторов с задержкой 1s/2s. Fallback для невалидного `layoutType` уже был в `resolveCanvasLayout` — проверено и задокументировано.
- **4.1 PDF-экспорт**: установлены `html2canvas-pro` + `jspdf`. Создан `lib/pdf-export.ts` (`exportPdf`, `downloadBlob`). В `editor-screen.tsx` — скрытый `PdfRenderHost` с render всех слайдов для html2canvas, кнопка «PDF» в топбаре с прогрессом.
- **4.2 Полировка дизайна**: в `slide-layouts.css` добавлены глобальные guard-правила: `overflow-wrap: anywhere; word-break: break-word; hyphens: auto` на все текстовые элементы слайдов; `min-height: 0` на body-зоны; cap `font-weight: 400` для body-текста. Плейсхолдеры уже были стилизованы через `border-style: dashed; opacity: 0.65` — без изменений. Тени/бордеры по шаблонам уже были корректны.
- **4.3 Адаптивность**: расширен `responsive.css` — брейкпоинт 961-1200px (rail горизонтально сверху), 721-960px (rail скрыт). В `editor-screen.tsx` добавлены touch swipe-обработчики для presenter mode (свайп влево/вправо переключает слайды).
- **4.5c Тесты**: добавлен `tests/pdf-export.test.ts` (2 теста — загрузка модуля и сигнатура функции). Скрипт `test:unit` обновлён, 31 тест — все проходят. Lint и typecheck чистые.

**Статус слоя** Внятно

**Что стало внятнее**
- PDF-экспорт работает через клиентский html2canvas+jsPDF без зависимости от серверной инфраструктуры.
- Ошибки рендера слайдов изолированы через ErrorBoundary — приложение не крашит.
- Retry при 502/504 снижает вероятность потери генерации из-за временного сбоя OpenAI.
- Русскоязычные длинные слова больше не переполняют карточки.
- Presenter mode работает на тач-устройствах.

**Что ещё мутно**
- PDF через html2canvas имеет ограничения: фоновые изображения с CORS могут не рендериться без дополнительной настройки. Для production-качества нужен серверный Puppeteer.
- Responsive-правки для 961-1200px и 721-960px не тестированы визуально — требуют проверки на реальных брейкпоинтах.

**Передача смены**
- PDF: `lib/pdf-export.ts` + `PdfRenderHost` в `editor-screen.tsx`. Точка входа — кнопка «PDF» в топбаре редактора.
- ErrorBoundary: `components/ui/error-boundary.tsx`.
- Retry: `callOpenAIWithRetry` в `lib/draft-adapter.ts`.
- Тесты: `npm run test:unit` покрывает 31 кейс.

---

### 054 Анализ конкурентов

**Что уложено**
- Собран мастер-промпт для большого анализа конкурентов.
- Подготовлена короткая версия промпта и список полей для подстановки перед запуском.
- Короткая версия будет пересобрана под текущий проект: AI-сервис рабочих презентаций `Внятно`, а не как универсальный шаблон без контекста продукта.

**Статус слоя** Внятно

**Что стало внятнее**
- Зафиксированы номер смены и короткое имя чата.
- Понятна цель слоя: получить промпт, который собирает сравнение конкурентов по сильным и слабым сторонам, рискам, дифференциаторам и выводам без лишнего шума.
- Пользователь может сразу запустить исследование, просто подставив рынок, географию и список компаний.
- Уточнён следующий шаг: дать короткий промпт уже с контекстом текущего продукта и его рабочей задачи.

**Что ещё мутно**
- Без списка компаний и географии исследование всё ещё останется частично общим; это лучше перед запуском дописать прямо в промпт.

**Передача смены**
- Если следующий шаг — уже реальный анализ, взять этот промпт и подставить: рынок, ЦА, географию, 5-10 конкурентов и критерии сравнения.

**Проблемы**
- Нет.

---

### 055 Clarify через чат

**Что уложено**
- Убран экран-wizard «Уточним главное» (5 фиксированных шагов).
- Экран `clarify` переделан в чат-карточку: лента сообщений, quick reply chips, ComposeField, CTA «Собрать черновик».
- Подключён `handleClarifySend` в оркестраторе (был написан, но не прокинут в UI).
- Wizard-стили (`.clarify-progress`, `.clarify-step`, `.clarify-chip` и др.) удалены из `chat.css`.
- Серверная часть не менялась — `createClarifySession` и `appendClarifyToSession` работали правильно.

**Статус слоя** Идти можно

**Что стало внятнее**
- Экран уточнения теперь соответствует `SCREEN_ATLAS.md` — чат-карточка с перепиской, а не форма.
- Убрана искусственная цепочка из 5 шагов, которая создавала барьер входа.
- `session.quickReplies` из сервера теперь показываются как кликабельные chips — пользователь может добрать контекст без набора текста.
- `session.readyToGenerate` управляет акцентом CTA — кнопка «Собрать черновик» становится заметнее когда информации достаточно.
- `handleClarifySend` был написан в оркестраторе с первого дня, но не подключён к UI — теперь подключён.

**Что ещё мутно**
- Не проверено на реальных данных от API: нужно убедиться, что `quickReplies` приходят непустыми при типичных запросах.

**Передача смены**
- Запустить приложение и пройти сценарий: промпт → уточнение через чат → «Собрать черновик».
- Файлы затронуты: `components/prototype/clarify-screen.tsx`, `components/presentation-prototype.tsx`, `app/styles/chat.css`.

**Проблемы**
- Нет.
